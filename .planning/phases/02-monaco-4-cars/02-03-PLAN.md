---
phase: 02-monaco-4-cars
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [game.js]
autonomous: false
requirements: [TRACK-02, TRACK-03]

must_haves:
  truths:
    - "El entorno de Mónaco se ve en bloques de color: agua del puerto (azul), edificios casino (gris/blanco), barreras armco (rojo/blanco)"
    - "Cuando un auto entra al túnel, se oscurece con un overlay; al salir, el overlay desaparece"
    - "Todos los autos permanecen visibles en el canvas incluso cuando están fuera de pista (BUG-OFFTRACK corregido)"
  artifacts:
    - path: "game.js"
      provides: "drawTrack() con ambientación de Mónaco, drawTunnelRoof() overlay, render de autos sin guard de off-track"
      contains: "drawTunnelRoof"
  key_links:
    - from: "drawTunnelRoof()"
      to: "loop() render"
      via: "se llama DESPUES de todos los drawCar()"
      pattern: "drawTunnelRoof"
    - from: "drawCar()"
      to: "render de todos los autos"
      via: "ningun guard de posición impide dibujar un auto off-track"
      pattern: "function drawCar"
---

<objective>
Dibujar la ambientación de Mónaco en bloques de color Canvas 2D (agua del puerto, edificios casino, barreras armco, boxes), implementar el overlay del túnel (polígono oscuro dibujado después de los autos), y corregir BUG-OFFTRACK (los autos deben permanecer visibles fuera de pista).

Purpose: La Fase 2 exige que Mónaco sea reconocible (TRACK-02), que el túnel oscurezca los autos (TRACK-03), y el usuario reportó que los autos desaparecen al salirse de pista (BUG-OFFTRACK, mandatorio).
Output: `game.js` con drawTrack() ambientado, drawTunnelRoof() en el orden de render correcto, y todos los autos siempre visibles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-monaco-4-cars/02-RESEARCH.md
@CLAUDE.md
@game.js
@.planning/phases/02-monaco-4-cars/02-02-SUMMARY.md

<interfaces>
<!-- Funciones de render que este plan modifica/crea. -->
drawTrack()         (game.js:377)   : dibuja ground + kerb + tarmac + racing line + meta + watermark
drawSpinePath()     (game.js:369)   : path de ROAD_SPINE proyectado (reusar para armco)
drawCar(car,...)    (game.js:442)   : dibuja un auto; SIN guard de off-track (verificar)
project(wx,wy)      (game.js:356)   : proyeccion isometrica world->screen
drawOffTrackVignette(alpha) (game.js:763) : viñeta roja radial cuando el jugador esta fuera de pista
loop() render solo  : 02-02 dejo el orden de dibujo de los 4 autos en cars.forEach/reverse
ROAD_SPINE, ROAD_HALF_W : geometria de Monaco instalada en 02-02
</interfaces>

<bug_offtrack_notes>
BUG-OFFTRACK (mandatorio, del usuario): cuando un auto sale de la pista, pierde visibilidad en el canvas.
La causa raíz NO es un guard explícito en drawCar() (verificado: drawCar siempre dibuja).
Causas candidatas a investigar:
1. La viñeta off-track (drawOffTrackVignette, alpha 0.55, game.js:887) se dibuja sobre todo el canvas y puede oscurecer/tapar al auto si se dibuja DESPUES del auto y con suficiente alpha en los bordes.
2. updateCar/updateAI podría empujar coordenadas a valores fuera del rango proyectable (auto sale del canvas).
3. El nuevo overlay del túnel, si se dibuja sobre toda la zona, puede tapar autos que no están en el túnel.
El executor DEBE diagnosticar la causa real reproduciéndola y corregirla para que todo auto permanezca visible off-track.
</bug_offtrack_notes>

<tunnel_reference>
Overlay del túnel (TRACK-03): enfoque recomendado en 02-RESEARCH.md (líneas 360-380, "polygon roof").
Dibujar un polígono oscuro (rgba ~0.62-0.70) proyectando las 4 esquinas de la bounding box del túnel, DESPUES de todos los drawCar(). Zona de túnel [ASSUMED] depende de la geometría final de 02-02; co-definir las constantes TUNNEL_ZONE leyendo dónde quedó el tramo de túnel en ROAD_SPINE.
</tunnel_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ambientación de Mónaco en drawTrack() y overlay del túnel</name>
  <files>game.js</files>
  <read_first>
    - game.js (drawTrack 377-433, drawSpinePath 369-375, project 356-359, loop render solo 819-949)
    - .planning/phases/02-monaco-4-cars/02-02-SUMMARY.md (coordenadas finales de ROAD_SPINE y zona de túnel tras la validación visual)
    - .planning/phases/02-monaco-4-cars/02-RESEARCH.md (seccion "Pattern 3: Monaco Environment Drawing" 572-612, "Tunnel Overlay" 328-391, "Monaco Environment Visual Reference" 784-802)
  </read_first>
  <action>
    Modificar `drawTrack()` (game.js:377-433) para dibujar la ambientación de Mónaco ANTES de la línea del spine (el tarmac del spine pinta encima y define las secciones). Cambiar el ground base de verde (`#1a4a10`) a gris ciudad (`#3a3a4a`). Agregar bloques de color con `fillRect`/`beginPath+fill`:
    - Agua del puerto: bloque azul (`#1a4a7a`) en la región del puerto (lado donde quedó la chicana/Swimming Pool según ROAD_SPINE de 02-02). Proyectar las esquinas con project() o usar un rect en screen-space que cubra esa zona.
    - Edificios casino: bloque gris/blanco (`#c8c8c4`) en la zona alta donde quedó Casino/Mirabeau.
    - Barreras armco: reusar `drawSpinePath()` con un lineWidth mayor que el kerb, alternando rojo (`#dc2626`) y blanco (`#f8fafc`) en dash — o stripes delgados a ambos lados del spine. Deben verse como barreras a lo largo del trazado.
    - Boxes / pit lane: franja de tarmac oscuro (`#1a1a1a`) junto a la recta principal.
    Conservar el kerb dasheado, el tarmac, la racing line y la línea de meta (ya reapuntada a CP0 en 02-02). El watermark ya dice Mónaco (02-02).

    Crear la función `drawTunnelRoof()` (nueva, cerca de drawTrack). Definir constantes `TUNNEL_ZONE = {x1, y1, x2, y2}` en world-space que cubran el tramo de túnel del ROAD_SPINE final (leer del SUMMARY de 02-02 qué puntos forman el túnel). Proyectar las 4 esquinas con `project()`, dibujar un polígono relleno con `ctx.fillStyle` oscuro (`#0d0d1a`) y `ctx.globalAlpha` ~0.68 dentro de un `save()`/`restore()`. Además marcar `car.inTunnel` por auto (boolean) comparando su posición world contra TUNNEL_ZONE, para uso futuro de audio en Fase 3.

    Integrar `drawTunnelRoof()` en el `loop()`: llamarla DESPUÉS de todos los `drawCar()` y ANTES del HUD/viñeta, en las fases `racing` y `done` (Pitfall 4: el roof debe pintarse sobre los autos, no debajo). En la fase `countdown` no es necesario.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('game.js','utf8'); if(!/function drawTunnelRoof/.test(s)){console.error('FAIL: falta drawTunnelRoof');process.exit(1)} if(!/TUNNEL_ZONE/.test(s)){console.error('FAIL: falta TUNNEL_ZONE');process.exit(1)} const racing=s.slice(s.indexOf(\"phase === 'racing'\")); const dc=racing.indexOf('drawCar'); const tr=racing.indexOf('drawTunnelRoof'); if(dc===-1||tr===-1||tr<dc){console.error('FAIL: drawTunnelRoof no se llama despues de drawCar en racing');process.exit(1)} console.log('OK tunnel roof tras drawCar')"</automated>
    <human-check>En VS CPU, conducir un auto por el tramo del túnel: el auto se oscurece mientras está bajo el polígono y recupera color al salir. El polígono del túnel se ve solo sobre el tramo del túnel, no sobre el resto de la pista.</human-check>
  </verify>
  <acceptance_criteria>
    - `drawTrack()` cambia el ground base a un gris (no verde `#1a4a10`) y dibuja al menos 3 bloques de color de ambientación (agua azul, casino gris/blanco, boxes/armco).
    - Existe la función `drawTunnelRoof()` y la constante `TUNNEL_ZONE` con 4 límites world-space.
    - En la fase `racing` del loop, la llamada a `drawTunnelRoof()` aparece DESPUÉS de las llamadas a `drawCar()`.
    - Cada auto tiene un boolean `car.inTunnel` actualizado según TUNNEL_ZONE.
    - Verificación manual: el auto se oscurece en el túnel y recupera color al salir.
  </acceptance_criteria>
  <done>Mónaco ambientado en bloques de color; overlay del túnel dibujado sobre los autos en el orden correcto.</done>
</task>

<task type="auto">
  <name>Task 2: Corregir BUG-OFFTRACK — autos siempre visibles fuera de pista</name>
  <files>game.js</files>
  <read_first>
    - game.js (drawCar 442-521, drawOffTrackVignette 763-771, loop fase racing render 879-922, updateCar 611-634, updateAI 639-676)
    - .planning/phases/02-monaco-4-cars/02-03-PLAN.md (bloque bug_offtrack_notes de este mismo plan)
  </read_first>
  <action>
    Diagnosticar y corregir BUG-OFFTRACK: cuando un auto sale de la pista pierde visibilidad. Pasos:

    1. Reproducir: en VS CPU, sacar el auto del jugador fuera de la pista y observar la consola y el canvas para confirmar el síntoma exacto (auto invisible, parpadea, o se va del canvas).

    2. Diagnosticar la causa entre las candidatas (ver bug_offtrack_notes): (a) la viñeta off-track con alpha alto tapa al auto si se dibuja después; (b) las coordenadas del auto exceden el rango proyectable y el auto sale del canvas; (c) el nuevo overlay del túnel tapa autos fuera del túnel; (d) algún guard introducido en el render por car.

    3. Corregir según la causa real. Garantías mínimas que la corrección DEBE cumplir:
       - `drawCar()` nunca debe omitir el dibujo de un auto por su posición de pista (no agregar guards de isOnTrack en el path de dibujo).
       - La viñeta off-track (`drawOffTrackVignette`) debe dibujarse de forma que NO oculte los autos: o bien dibujarla ANTES de los drawCar(), o mantenerla como gradiente radial cuyo centro (donde está la acción) sea transparente (el gradiente actual ya es transparente en el centro — verificar que el auto del jugador no quede en la zona opaca del borde). Si la viñeta es la causa, moverla en el orden de render para que los autos se pinten por encima.
       - Si las coordenadas se salen del world-space, clampear suavemente la posición de cada auto en `updateCar`/`updateAI` o asegurar que `project()` siga produciendo coordenadas dentro del canvas para autos cercanos al borde de pista (los autos off-track siguen sobre asfalto/escapatoria dentro del canvas, no en el void).
       - El overlay del túnel solo debe oscurecer (no ocultar) y solo dentro de TUNNEL_ZONE.

    4. Confirmar que TODOS los autos (jugador y los 3 IA) permanecen visibles cuando están fuera de pista, en las fases racing y done.

    Documentar en el SUMMARY la causa raíz encontrada y la corrección aplicada.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('game.js','utf8'); const dc=s.slice(s.indexOf('function drawCar'),s.indexOf('function drawCar')+1600); if(/isOnTrack[\s\S]{0,40}return/.test(dc)){console.error('FAIL: drawCar tiene guard de off-track que omite el dibujo');process.exit(1)} console.log('OK drawCar sin guard de off-track')"</automated>
    <human-check>En VS CPU, sacar el auto del jugador fuera de la pista en varias zonas: el auto permanece visible en todo momento (con la viñeta roja de fondo pero el auto encima). Forzar que un auto IA se salga: también permanece visible. Repetir en la pantalla de resultados (fase done).</human-check>
  </verify>
  <acceptance_criteria>
    - `drawCar()` no contiene ningún `return` temprano basado en `isOnTrack` ni en la posición de pista.
    - El orden de render garantiza que los autos se pintan por encima de la viñeta off-track O la viñeta no tiene zona opaca sobre la posición de los autos.
    - Verificación manual: el auto del jugador permanece visible fuera de pista en cualquier zona; los autos IA también; tanto en racing como en done.
    - El SUMMARY documenta la causa raíz real y la corrección.
  </acceptance_criteria>
  <done>Todos los autos permanecen visibles fuera de pista; causa raíz de BUG-OFFTRACK identificada y corregida.</done>
</task>

</tasks>

<verification>
- drawTrack() muestra ambientación de Mónaco en bloques de color (agua, casino, armco, boxes).
- drawTunnelRoof() existe, usa TUNNEL_ZONE y se llama después de drawCar() en racing y done.
- El auto se oscurece en el túnel y recupera color al salir.
- BUG-OFFTRACK: todos los autos permanecen visibles fuera de pista (verificación manual).
</verification>

<success_criteria>
Mónaco es visualmente reconocible con bloques de color. El túnel oscurece los autos que pasan por él mediante un overlay dibujado sobre ellos. Ningún auto desaparece al salirse de la pista.
</success_criteria>

<output>
Crear `.planning/phases/02-monaco-4-cars/02-03-SUMMARY.md` al terminar (incluir causa raíz de BUG-OFFTRACK).
</output>
