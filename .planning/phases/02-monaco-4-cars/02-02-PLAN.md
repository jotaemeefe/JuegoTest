---
phase: 02-monaco-4-cars
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [game.js]
autonomous: false
requirements: [TRACK-01, TRACK-04]

must_haves:
  truths:
    - "La pista se ve como Mónaco: hay una horquilla cerrada (Grand Hotel), un tramo recto de túnel, y curvas reconocibles"
    - "El trazado forma un circuito cerrado: los autos pueden completar una vuelta entera sin salirse en la línea de meta"
    - "Los 3 autos IA navegan el trazado de Mónaco completo sin quedarse trabados ni salirse permanentemente"
    - "El jugador puede completar 3 vueltas pasando los 4 checkpoints en orden en el nuevo trazado"
  artifacts:
    - path: "game.js"
      provides: "ROAD_SPINE de Mónaco (polilínea), AI_WAYPOINTS de Mónaco, CPS de Mónaco, START de 4 autos"
      contains: "ROAD_SPINE"
  key_links:
    - from: "ROAD_SPINE[0]"
      to: "ROAD_SPINE[ultimo]"
      via: "el primer y ultimo punto coinciden para cerrar el loop"
      pattern: "ROAD_SPINE"
    - from: "AI_WAYPOINTS"
      to: "updateAI(car) via car.wpIdx"
      via: "waypoints de Monaco guian a la IA"
      pattern: "AI_WAYPOINTS"
    - from: "CPS"
      to: "checkCheckpoints"
      via: "4 checkpoints posicionados sobre el trazado de Monaco"
      pattern: "const CPS"
---

<objective>
Reemplazar la geometría del óvalo (`ROAD_SPINE`, `AI_WAYPOINTS`, `CPS`, `START`) por el trazado de Mónaco en sentido horario, ajustado al canvas world-space de 480×640. Co-diseñar los 4 checkpoints sobre el nuevo trazado.

Purpose: Mónaco reemplaza el óvalo por completo (decisión bloqueada en STATE.md). Es la base geométrica que `isOnTrack()`, `drawTrack()`, `updateAI()` y `checkCheckpoints()` leen. Toda la ambientación y el túnel (02-03) dependen de esta geometría.
Output: `game.js` con las constantes de geometría de Mónaco; la pista renderiza un trazado reconocible y los 4 autos pueden completar vueltas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-monaco-4-cars/02-RESEARCH.md
@CLAUDE.md
@game.js
@.planning/phases/02-monaco-4-cars/02-01-SUMMARY.md

<interfaces>
<!-- Constantes geométricas actuales (óvalo) que este plan reemplaza. -->
ROAD_SPINE   (game.js:16-35)  : array de [x,y] world-space; primer punto == ultimo (cierra loop)
ROAD_HALF_W  (game.js:15)     : 60 — mantener
CPS          (game.js:38-43)  : array de {x,y,r}; CP0 = META/finish
START        (game.js:47-50)  : array de {x,y,a}; el plan 02-01 ya lo extendio a 4 entradas
AI_WAYPOINTS (game.js:97-116) : array de [x,y] CW; consumido por updateAI via car.wpIdx
AI_WP_REACH  (game.js:637)    : 45 — radio para avanzar de waypoint

Consumidores (NO cambian su algoritmo, solo leen los datos nuevos):
- isOnTrack(x,y)     (game.js:543) : recorre segmentos de ROAD_SPINE
- drawSpinePath()    (game.js:369) : dibuja ROAD_SPINE proyectado
- project(wx,wy)     (game.js:356) : proyeccion isometrica; world-space 0..480 x 0..640
</interfaces>

<monaco_geometry_reference>
Polilínea de Mónaco propuesta (28 puntos, [ASSUMED] en 02-RESEARCH.md líneas 131-158).
Checkpoints propuestos (líneas 169-174). START de 4 autos (líneas 187-192).
Dirección: HORARIA. Landmarks que deben ser reconocibles: horquilla Grand Hotel (casi U), tramo de túnel horizontal, secuencia de chicana del puerto, Rascasse antes de la recta principal.
</monaco_geometry_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reemplazar ROAD_SPINE, CPS, START y AI_WAYPOINTS por la geometría de Mónaco</name>
  <files>game.js</files>
  <read_first>
    - game.js (constantes de geometría líneas 14-116; función isOnTrack 543-550; drawTrack 377-433)
    - .planning/phases/02-monaco-4-cars/02-RESEARCH.md (seccion "Monaco Circuit Geometry", líneas 94-195, y "Common Pitfalls" 2/5/7, líneas 650-691)
  </read_first>
  <action>
    Reemplazar el contenido del array `ROAD_SPINE` (game.js:16-35) por la polilínea de Mónaco de 28 puntos especificada en 02-RESEARCH.md (líneas 137-158). REQUISITO CRÍTICO: el último punto del array debe ser idéntico al primero `[60, 550]` para cerrar el loop (Pitfall 2). Actualizar el comentario del bloque para describir Mónaco horario en lugar del óvalo de Buenos Aires. Mantener `ROAD_HALF_W = 60`.

    Reemplazar el array `CPS` (game.js:38-43) por los 4 checkpoints de Mónaco de 02-RESEARCH.md (líneas 169-174): CP0 META en la recta principal {x:130,y:550,r:80}, CP1 zona Casino/Mirabeau {x:360,y:265,r:70}, CP2 horquilla Grand Hotel {x:370,y:193,r:65}, CP3 salida de túnel {x:415,y:310,r:70}. Mantener radios entre 65 y 80 (Pitfall: con ROAD_HALF_W=60 radios menores pueden no detectar autos al borde).

    Reemplazar el array `START` (game.js:47-50, ya extendido a 4 entradas en 02-01) por la grilla de 4 autos de Mónaco de 02-RESEARCH.md (líneas 187-192): formación 2×2 sobre la recta principal apuntando al este (a:0), con ~30px de separación lateral y ~6px longitudinal. VERIFICAR que cada posición de salida satisface `isOnTrack(x,y)` con la nueva ROAD_SPINE antes de dar por terminada la tarea (Pitfall 7 — autos no deben empezar fuera de pista ni colisionando).

    Reemplazar el array `AI_WAYPOINTS` (game.js:97-116) por un conjunto de waypoints derivados del trazado de Mónaco. Generar 22-25 waypoints siguiendo la polilínea ROAD_SPINE en sentido horario, con mayor densidad en las curvas cerradas (Grand Hotel Hairpin, Rascasse, chicana del puerto) — Open Question 2 del research recomienda 22-25 puntos. Los waypoints deben estar sobre el eje del trazado (a menos de ROAD_HALF_W del spine). Reducir `AI_WP_REACH` (game.js:637) de 45 a 30 para curvas más cerradas de Mónaco.

    Actualizar la línea de meta proyectada en `drawTrack()` (game.js:409-426): las coordenadas hardcodeadas `project(210, 527 - ROAD_HALF_W)` y `project(210, 527 + ROAD_HALF_W)` deben apuntar a la nueva posición de CP0 (x:130, y:550). Actualizar el watermark (game.js:429-432) de "BUENOS AIRES" a "MÓNACO" / "MONTE CARLO".

    Ejecutar una verificación de bounds: proyectar todos los puntos de ROAD_SPINE con `project()` mentalmente o vía un check rápido y confirmar que caen dentro de [0,480]×[0,640] (Pitfall 5). Si algún punto se sale, ajustar la coordenada world-space hacia adentro.

    NO incluir bloques de código cerrados en este plan; trabajar con Edit sobre game.js.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('game.js','utf8'); const m=s.match(/const ROAD_SPINE = \[([\s\S]*?)\];/); if(!m){console.error('FAIL: no ROAD_SPINE');process.exit(1)} const pts=[...m[1].matchAll(/\[\s*(-?\d+)\s*,\s*(-?\d+)\s*\]/g)].map(x=>[+x[1],+x[2]]); if(pts.length<10){console.error('FAIL: muy pocos puntos');process.exit(1)} const a=pts[0],b=pts[pts.length-1]; if(a[0]!==b[0]||a[1]!==b[1]){console.error('FAIL: loop no cerrado',a,b);process.exit(1)} const oob=pts.filter(p=>p[0]<0||p[0]>480||p[1]<0||p[1]>640); if(oob.length){console.error('FAIL: puntos fuera de bounds',oob);process.exit(1)} console.log('OK ROAD_SPINE cerrado y en bounds,',pts.length,'puntos')"</automated>
  </verify>
  <acceptance_criteria>
    - `ROAD_SPINE` tiene >= 20 puntos y su primer punto es idéntico al último (loop cerrado).
    - Todos los puntos de `ROAD_SPINE` caen dentro de [0,480]×[0,640].
    - `CPS` tiene exactamente 4 entradas, todas con `r` entre 65 y 80.
    - `START` tiene 4 entradas y cada una satisface `isOnTrack(x,y)` con la nueva ROAD_SPINE.
    - `AI_WAYPOINTS` tiene entre 22 y 25 puntos.
    - `AI_WP_REACH` vale 30.
    - El watermark en `drawTrack()` dice MÓNACO/MONTE CARLO, no Buenos Aires.
  </acceptance_criteria>
  <done>Geometría de Mónaco instalada; loop cerrado; checkpoints y grilla sobre pista; waypoints densificados.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    El trazado del óvalo fue reemplazado por la polilínea de Mónaco (28 puntos, horario), con 4 checkpoints, grilla de salida de 4 autos y 22-25 waypoints de IA. Las coordenadas son una primera aproximación [ASSUMED] del research y requieren validación visual humana (Open Question 1 del research recomienda 2-3 rondas de iteración).
  </what-built>
  <how-to-verify>
    1. Abrir index.html en el navegador (o servir con `npx http-server . -p 8081`).
    2. Click en "VS CPU 🤖", elegir cualquier rival, iniciar la carrera.
    3. Observar el trazado dibujado en el canvas. Verificar que se reconocen: (a) una horquilla muy cerrada tipo U (Grand Hotel) en la zona superior-derecha, (b) un tramo recto que será el túnel, (c) la secuencia de chicana del puerto en la parte baja, (d) Rascasse como curva cerrada antes de volver a la recta principal.
    4. Verificar que los 4 autos arrancan sobre la pista (no fuera, no encimados) y que al soltar, los 3 autos IA siguen el trazado sin salirse permanentemente ni quedarse trabados en una esquina.
    5. Conducir el auto del jugador una vuelta completa: confirmar que se pueden pasar los 4 checkpoints en orden y cruzar la meta sin que `isOnTrack` falle en la recta principal.
    6. Si la forma NO se parece a Mónaco o algún auto IA se sale o se traba: describir qué corregir (qué curva, qué coordenada aproximada). El executor ajustará las coordenadas y repetirá (presupuestar 2-3 rondas).
  </how-to-verify>
  <resume-signal>Escribir "aprobado" si el trazado es reconocible y jugable, o describir los ajustes de coordenadas necesarios.</resume-signal>
</task>

</tasks>

<verification>
- ROAD_SPINE cierra el loop (primer punto == último) y todos los puntos caen en bounds.
- CPS tiene 4 entradas con radios 65-80; START tiene 4 posiciones on-track.
- AI_WAYPOINTS tiene 22-25 puntos; AI_WP_REACH = 30.
- Validación humana: trazado reconocible como Mónaco; 4 autos completan vueltas.
</verification>

<success_criteria>
El óvalo está reemplazado por el trazado de Mónaco. El circuito es un loop cerrado dentro del canvas. Los 4 checkpoints permiten completar vueltas. Los autos IA navegan el trazado completo. La forma es reconocible como Mónaco tras la validación visual.
</success_criteria>

<output>
Crear `.planning/phases/02-monaco-4-cars/02-02-SUMMARY.md` al terminar.
</output>
