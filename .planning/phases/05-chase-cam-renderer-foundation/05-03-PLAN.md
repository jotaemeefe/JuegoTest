---
phase: 05-chase-cam-renderer-foundation
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: [game.js, assets/kart-chase.png]
autonomous: true
requirements: [RENDER-01]

must_haves:
  truths:
    - "The player kart is drawn from behind as an early illustrated (non-pixel-art) sprite in 3-5 turn-angle frames, fixed low-center in the frame"
    - "The kart frame is selected (not rotated) by current steering input"
    - "Rival cars render as depth-scaled sprites positioned by their track z relative to the camera, scaling smoothly with no size pop-in"
    - "Both duel (P1/2) and grand-prix (P1/22) fields render their traffic correctly in the chase-cam view"
  artifacts:
    - path: "assets/kart-chase.png"
      provides: "Early illustrated chase-view kart/pilot sprite sheet, 3-5 turn-angle frames, bright/warm palette"
    - path: "game.js"
      provides: "drawKartSprite compositor (frame-select + continuous depth scale) replacing drawCar's rotation-matrix technique"
      contains: "function drawKartSprite"
  key_links:
    - from: "rival world position"
      to: "screen scale/position"
      via: "continuous z-distance to camera (not segment-index snap)"
      pattern: "drawKartSprite"
    - from: "carStyle(car, idx)"
      to: "sprite variant/tint"
      via: "reused per-car style resolution ladder"
      pattern: "carStyle"
---

<objective>
Give the game its first-playable visual identity: an early illustrated (non-pixel-art) chase-view kart/pilot sprite (D-09) and a sprite compositor that draws the player kart fixed low-center with frame-selection by steering, and draws rival cars as continuously depth-scaled sprites positioned by their track distance (avoiding the classic pop-in of PITFALLS.md Pitfall 3).

Purpose: The chase-cam road (Plan 02) is not playable-feeling without the kart in front of the camera and rivals scaling smoothly as traffic. D-09 wants a real illustrated attempt now (not a placeholder box) to validate the view-angle-frame approach the whole art pipeline depends on going into Phase 8.
Output: A new illustrated kart asset plus a drawKartSprite compositor replacing drawCar's rotation technique, wired into all three render blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-chase-cam-renderer-foundation/05-CONTEXT.md
@.planning/phases/05-chase-cam-renderer-foundation/05-PATTERNS.md
@.planning/phases/05-chase-cam-renderer-foundation/05-UI-SPEC.md
@.planning/research/PITFALLS.md
@.planning/phases/05-chase-cam-renderer-foundation/05-02-SUMMARY.md

<interfaces>
<!-- Consumed from Plan 02 + existing codebase -->
- projectSegment(seg, cameraZ, cameraX, cameraY, cameraDepth) , renderRoad(cameraZ, cameraX) , segmentAt(z) , CAMERA_DEPTH , DRAW_DISTANCE (Plan 02)
- carStyle(car, carIdx) : returns { body, stripe, cockpit, helmet, num } — player/remote/AI/fallback resolution ladder (game.js:1039)
- nearestSpinePoint(x, y) , SPINE_CUMLEN : world position → arc-length (for a rival's track z)
- car fields: x, y, angle, velAngle, speed, finished, flashUntil, isPlayer (world-space, authoritative per D-03)
- keys.left / keys.right : current steering input (for player frame selection)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce the early illustrated chase-view kart/pilot sprite (D-09)</name>
  <read_first>
    - .planning/phases/05-chase-cam-renderer-foundation/05-CONTEXT.md — D-06 (Silksong technique, bright/warm palette, NOT gothic), D-09 (early illustrated attempt, not a placeholder box; may be refined in Phase 8), Claude's Discretion note (rough style OK)
    - .planning/phases/05-chase-cam-renderer-foundation/05-UI-SPEC.md — Art direction guardrails, Color section (bright/warm palette; accent gold #F4A93A)
    - .planning/phases/05-chase-cam-renderer-foundation/05-PATTERNS.md — "No Analog Found" table (illustrated kart is a genuinely new art asset)
    - game.js lines 682-696 (Image()/onload slice-into-offscreen-canvas loading pattern to mirror for the new sprite sheet)
  </read_first>
  <action>
    Create assets/kart-chase.png: an early illustrated (hand-drawn/painterly, textured — Silksong TECHNIQUE, explicitly NOT gothic mood) rear view of the kart+pilot, in a bright/warm palette consistent with "sunny day of karting in Argentina" (D-06). Provide 3-5 discrete turn-angle frames laid out horizontally as equal-width cells (e.g. hard-left, left, straight, right, hard-right), each showing the kart leaning/angled for that steer state as seen from behind. Use the Colapinto/Alpine identity colors as accents (the existing player style is body #0090d0 / stripe #f569b7 / helmet #74c0fc) but rendered illustrated, not flat-fill. Load it in game.js mirroring the existing tileSheet loading idiom (new Image(); onload slices the sheet into an array of per-frame offscreen canvases; guard all draws with a null-check fallback to the existing flat-fill drawCar so the game never renders nothing if the asset is missing). Keep the asset modest in size — it is explicitly allowed to be rough and will be refined/replaced in Phase 8; do not spend budget on polish. If pixel-perfect illustration cannot be authored programmatically, a canvas-drawn painterly composite (gradients/shadows/highlights, not flat rectangles) generated at load into the frame canvases is an acceptable substitute for the PNG, as long as it reads as illustrated and non-pixel-art.
  </action>
  <verify>
    <automated>cd "c:/Users/Julio/OneDrive/Documents/GitHub/JuegoTest" && (ls -la assets/kart-chase.png 2>/dev/null || echo "PNG absent — canvas-generated frames path"); grep -nE "kart-chase|kartFrames|kartSheet" game.js</automated>
  </verify>
  <acceptance_criteria>
    - Either assets/kart-chase.png exists OR game.js generates illustrated frame canvases at load (non-pixel-art, painterly) — grep shows a kartFrames/kartSheet loading path
    - The kart sprite provides 3-5 distinct turn-angle frames (a frame-index selector exists spanning that many cells)
    - The palette is bright/warm (uses the Colapinto blue/pink accents, no gray/gothic dominant) — no cool-gray dominant fill
    - Draws are guarded with a fallback to the existing drawCar flat-fill so a missing asset never renders empty
  </acceptance_criteria>
  <done>An illustrated rear-view kart with multiple steer frames is available to the compositor and visibly reads as "moving toward Silksong-technique, bright/warm" per D-09.</done>
</task>

<task type="auto">
  <name>Task 2: drawKartSprite compositor — fixed player, depth-scaled rivals</name>
  <read_first>
    - game.js lines 1039-1141 (carStyle style-resolution ladder to reuse; drawCar rotation-matrix + flash overlay + screen-space number to replace/reuse)
    - game.js lines 2059-2065, 2111-2117, 2137-2142 (the three drawCar call sites in racing/done/countdown render blocks)
    - .planning/research/PITFALLS.md — Pitfall 3 (continuous z-distance scaling, not segment-index snap; verify against a moving rival, not static props)
    - .planning/phases/05-chase-cam-renderer-foundation/05-PATTERNS.md — "Illustrated kart/pilot sprite compositor (D-09)" (reuse style ladder, replace rotation with frame-select, continuous depth scale is new)
    - .planning/phases/05-chase-cam-renderer-foundation/05-UI-SPEC.md — "Position badge range" note (P1/2 duel and P1/22 grand prix both in scope)
  </read_first>
  <action>
    Add drawKartSprite(car, carIdx, cameraZ, cameraX) that replaces drawCar's rotation-matrix technique (game.js:1055-1141). For the PLAYER (carIdx 0): draw the kart at a fixed low-center screen position (lower-center third, per D-10 framing), selecting the turn-angle frame from Task 1 by current steering (keys.left → left frame, keys.right → right frame, neither → straight; optionally a hard-turn frame past a small hold threshold) — select the frame, do NOT rotate it. For RIVAL/traffic cars: compute the rival's arc-length z via nearestSpinePoint + SPINE_CUMLEN, take its continuous z-distance relative to cameraZ (NOT snapped to segment index — Pitfall 3), cull if behind camera or beyond DRAW_DISTANCE, and project screen position + scale continuously with that distance (scale = CAMERA_DEPTH / relZ, same divide as the road) so approaching rivals grow smoothly; offset horizontally by the rival's lateral offset from the centerline. Reuse carStyle(car, carIdx) unchanged to pick each car's body/stripe/helmet variant/tint. Keep the existing overtake-flash overlay (car.flashUntil, game.js:1122-1130) and screen-space car-number text (game.js:1134-1140) — both are technique-agnostic. Draw rivals back-to-front (farthest first) then the player last, in all three render blocks; replace the three drawCar( call sites with drawKartSprite( (preserving the existing far-to-near draw order loop, now keyed on projected depth). Ensure both gameMode 'duel' (2 cars) and 'solo' (22 cars) iterate correctly.
  </action>
  <verify>
    <automated>cd "c:/Users/Julio/OneDrive/Documents/GitHub/JuegoTest" && grep -nE "function drawKartSprite" game.js; grep -c "drawKartSprite(" game.js; grep -c "drawCar(" game.js; grep -nE "carStyle\(" game.js | head</automated>
  </verify>
  <acceptance_criteria>
    - drawKartSprite is declared and called in all three render blocks (racing/done/countdown) — grep shows the compositor replaces the drawCar draw sites
    - Rival scale derives from continuous relZ (CAMERA_DEPTH / relZ), not from an integer segment index — verifiable in the function body
    - carStyle(car, carIdx) is still called for per-car appearance (style ladder reused)
    - The overtake-flash (car.flashUntil) and screen-space car-number draw are preserved
    - Player kart uses frame selection keyed on keys.left/keys.right (no rotation matrix applied to the player sprite)
  </acceptance_criteria>
  <done>Driving VS CPU shows the illustrated kart fixed ahead of the camera changing steer frames, with rival karts growing/shrinking smoothly as traffic in both duel and grand-prix fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Adds a same-origin static image asset and local rendering code. No user input, no network, no eval/innerHTML. The new PNG is loaded via Image() exactly like the existing tileset (same-origin, no canvas cross-origin taint). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Denial of Service | rival sprite loop | mitigate | Rival sprites are culled beyond DRAW_DISTANCE / behind camera before drawing, bounding sprite draw work; 22-car field is the existing proven ceiling. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs; the only new artifact is a static image asset in-repo. |
</threat_model>

<verification>
- The player kart renders illustrated (non-pixel-art), fixed low-center, frame-selected by steering (D-09/D-10).
- Rivals scale continuously with distance (no pop-in, Pitfall 3) in both duel and grand-prix.
- carStyle ladder, overtake flash and car-number text preserved.
- Missing asset degrades gracefully to flat-fill fallback.
</verification>

<success_criteria>
- RENDER-01: the chase-cam view now includes the kart drawn from behind (frame-selected) and depth-scaled traffic — completing the "third-person camera behind the kart" success criterion.
</success_criteria>

<output>
Create `.planning/phases/05-chase-cam-renderer-foundation/05-03-SUMMARY.md` when done.
</output>
