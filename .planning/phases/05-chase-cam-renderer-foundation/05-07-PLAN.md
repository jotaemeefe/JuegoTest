---
phase: 05-chase-cam-renderer-foundation
plan: 07
type: execute
wave: 7
depends_on: ["05-06"]
files_modified: []
autonomous: false
requirements: [RENDER-02, RENDER-03, RENDER-04]

must_haves:
  truths:
    - "The curve+crest test segment renders without kinks, V-shapes or self-intersection (RENDER-03 confirmed visually)"
    - "The game holds a playable framerate on real iOS Safari and a mid-tier Android device at the fixed draw-distance (RENDER-04)"
    - "The player can drive the test loop end-to-end with speed sensation present (RENDER-02)"
    - "Existing mobile/iOS fixes (lazy AudioContext, Pointer Events touch, devicePixelRatio sizing, viewport-fit) still work against the new renderer"
  artifacts: []
  key_links:
    - from: "real device profiling"
      to: "DRAW_DISTANCE sign-off"
      via: "human-verified framerate on iOS Safari + Android"
      pattern: "DRAW_DISTANCE"
---

<objective>
Human verification gate for the phase's feel-and-mobile requirements that cannot be proven by source assertions: confirm the curve+crest renders cleanly (RENDER-03 / Pitfall 1), the fixed draw-distance holds a playable framerate on REAL iOS Safari and a mid-tier Android device (RENDER-04 / Pitfall 2), speed sensation is present (RENDER-02), and no hard-won mobile/iOS fix regressed during the rewrite (Pitfall 9).

Purpose: RENDER-03 and RENDER-04 explicitly require visual correctness and real-device profiling — not desktop-throttled proxies. This is the "first playable version" acceptance gate (CONTEXT: "something a person can actually sit down and drive").
Output: A pass/fail sign-off; any failure becomes a gap-closure input.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-chase-cam-renderer-foundation/05-UI-SPEC.md
@.planning/research/PITFALLS.md
@.planning/phases/05-chase-cam-renderer-foundation/05-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Real-device playable + curve+crest + mobile-regression sign-off</name>
  <action>
    Human verification gate — no code changes. Serve the built game locally and verify on desktop plus real iOS Safari and a mid-tier Android device the six checks in how-to-verify: no console errors, curve+crest renders without kinking (RENDER-03), speed sensation scales with speed (RENDER-02), playable framerate at the fixed DRAW_DISTANCE on both phones (RENDER-04), no mobile/iOS regression (Pitfall 9), and first-playable end-to-end with multiplayer hidden (D-01/D-05). Any failure is captured for gap closure.
  </action>
  <what-built>
    The complete Phase 5 chase-cam renderer: a synthetic closed test loop with a deliberate curve+crest segment (Plan 01), a scanline segment projector with draw-distance culling and chase camera (Plan 02), an illustrated frame-selected kart with depth-scaled rivals (Plan 03), speed sensation cues (Plan 04), and adapted HUD with DRS/minimap removed and multiplayer hidden (Plan 05). All automated smoke tests pass (Plan 06).
  </what-built>
  <how-to-verify>
    1. Serve the game locally: run `npx http-server . -p 8081` and open http://localhost:8081 first on desktop to confirm no console errors.
    2. RENDER-03 (curve+crest): drive to the segment marked "RENDER-03 curve+crest test case" and confirm the road edges do NOT kink into a V, overlap, or self-intersect on the corner-that-is-also-a-crest; the horizon should move monotonically, not jump.
    3. RENDER-02 (speed sensation): accelerate to top speed and confirm the road visibly rushes faster, the rumble strips strobe faster, a subtle shake/vignette appears at high speed, and engine pitch rises with speed; then slow down and confirm all cues calm.
    4. RENDER-04 (mobile framerate): open the same URL on a REAL iOS Safari session AND a mid-tier Android device (over LAN IP), drive a full lap on each, and confirm the framerate stays playable (no stutter cliff) at the current DRAW_DISTANCE — this is the real-hardware profiling the requirement mandates, not desktop DevTools throttling. If either device stutters, note it (recovery = lower DRAW_DISTANCE / add batching, Plan 02 constants).
    5. Mobile regression (Pitfall 9): on the phone, confirm audio starts on first tap (lazy AudioContext), the on-screen ◀ ▶ FRENO buttons do not stick when a finger slides off (Pointer Events), the canvas is not cut off / no scrolling needed (viewport-fit + devicePixelRatio sizing).
    6. First-playable (D-01): confirm hitting "GRAND PRIX · 22 AUTOS" and "DUELO · 1 VS 1" both launch straight into the chase-cam view and are drivable end-to-end; confirm the lobby shows NO create/join buttons.
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks pass, or describe which check failed (and on which device) so a gap-closure plan can be scoped.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Verification-only plan; runs the already-built local game. No code changes, no network payload changes, no new input surface. The local static HTTP server serves same-origin files only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Information Disclosure | LAN http-server for device testing | accept | The dev server is a temporary local static server on the developer's LAN for device testing only; no secrets are served (client-side game). Stop it after testing. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | `npx http-server` is a dev-only static server invocation, not a project dependency; no package.json change, nothing added to the shipped game. |
</threat_model>

<verification>
- Curve+crest renders without kinking (RENDER-03), speed sensation present (RENDER-02), playable framerate on real iOS Safari + mid-tier Android at fixed draw-distance (RENDER-04), mobile fixes intact (Pitfall 9), first-playable end-to-end (D-01).
</verification>

<success_criteria>
- All four ROADMAP Phase 5 success criteria are confirmed on real hardware, or failures are captured for gap closure.
</success_criteria>

<output>
Create `.planning/phases/05-chase-cam-renderer-foundation/05-07-SUMMARY.md` when done (record device models tested, framerate observations, and any failed check).
</output>
