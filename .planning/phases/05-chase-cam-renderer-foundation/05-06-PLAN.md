---
phase: 05-chase-cam-renderer-foundation
plan: 06
type: execute
wave: 6
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
files_modified: [tests/chase-cam-smoke.test.js, tests/r4a-smoke.test.js, tests/r4b-release.test.js, .planning/phases/05-chase-cam-renderer-foundation/05-RELEASE.md, .planning/ROADMAP.md, CLAUDE.md]
autonomous: true
requirements: [RENDER-01, RENDER-04]

must_haves:
  truths:
    - "A chase-cam smoke test asserts the renderer invariants (projector present, draw-distance cap, SEGMENTS table, Monaco/DRS/minimap removed) and passes via node"
    - "The obsolete Monaco-era assertions in the existing smoke tests no longer fail against the new architecture"
    - "A phase RELEASE.md records scope, decisions, files/assets, compatibility constraints and validation, per the mandatory release documentation workflow"
    - "ROADMAP.md Phase 5 is updated with the finalized plan list; CLAUDE.md reflects the new chase-cam renderer"
  artifacts:
    - path: "tests/chase-cam-smoke.test.js"
      provides: "Source-assertion smoke test for the chase-cam renderer (runs via node)"
    - path: ".planning/phases/05-chase-cam-renderer-foundation/05-RELEASE.md"
      provides: "Phase 5 release record (mandatory workflow)"
  key_links:
    - from: "tests/chase-cam-smoke.test.js"
      to: "game.js renderer invariants"
      via: "source-string assertions like r4a-smoke.test.js"
      pattern: "projectSegment"
    - from: "CLAUDE.md"
      to: "new renderer architecture"
      via: "documentation update"
      pattern: "chase-cam"
---

<objective>
Lock in the phase with automated coverage and the mandatory release documentation. Add a chase-cam smoke test that asserts the new renderer's structural invariants (following the existing source-assertion test style), update the two Monaco-era smoke tests that would otherwise fail against the rewrite, and produce the phase RELEASE.md + ROADMAP.md + CLAUDE.md documentation the project requires for every implementation batch.

Purpose: The project's CLAUDE.md mandates a per-phase PLAN.md + RELEASE.md and a ROADMAP.md update in the same change. Automated smoke coverage guards the RENDER-01/RENDER-04 invariants against future regressions (and seeds the broader test work formally scoped for Phase 11).
Output: New/updated tests plus release documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-chase-cam-renderer-foundation/05-CONTEXT.md
@CLAUDE.md
@tests/r4a-smoke.test.js
@.planning/phases/05-chase-cam-renderer-foundation/05-01-SUMMARY.md
@.planning/phases/05-chase-cam-renderer-foundation/05-02-SUMMARY.md
@.planning/phases/05-chase-cam-renderer-foundation/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chase-cam smoke test + repair obsolete Monaco-era assertions</name>
  <read_first>
    - tests/r4a-smoke.test.js (full — the source-assertion test style: readFileSync game.js/index.html, assert.match/doesNotMatch, run via `node tests/<file> <root>`)
    - tests/r4b-release.test.js (full — second existing smoke test)
    - game.js after Plans 01-05 (grep for projectSegment, renderRoad, DRAW_DISTANCE, SEGMENTS, drawKartSprite, and confirm drawMinimap/updateDRSUI/buildEnvCanvas/drawTrack removal state)
    - .planning/phases/05-chase-cam-renderer-foundation/05-01-SUMMARY.md through 05-05-SUMMARY.md (what actually shipped)
  </read_first>
  <action>
    Create tests/chase-cam-smoke.test.js in the same style as tests/r4a-smoke.test.js (require fs/assert, read game.js and index.html via the process.argv[2] root, print an OK summary line). Assert the Phase 5 renderer invariants: game.js contains `function projectSegment`, `function renderRoad`, `const SEGMENTS`, `const DRAW_DISTANCE` (and that DRAW_DISTANCE is a number in a sane range, e.g. ≤ 200); game.js contains `function drawKartSprite`; game.js does NOT contain `function project(wx, wy)` (identity no-op gone), `function drawTrack`, `function drawMinimap`, `updateDRSUI`, `useDRS`, or the strings "CIRCUIT DE MONACO"/"Beau Rivage"; index.html does NOT contain `id="btn-drs"` and DOES contain `hidden` on #btn-create/#btn-join-screen; index.html tagline does not contain "PIXEL RACING". Then update the two existing smoke tests so they pass against the new architecture: in tests/r4a-smoke.test.js and tests/r4b-release.test.js, remove or replace the now-false Monaco/top-down assertions (e.g. the drawPixelGround / finalizePixelEnvironment / buildEnvCanvas / WORLD_SCALE-grid-at-620 checks and any DRS-button assertion) that Plans 01-05 invalidated, keeping only invariants that remain true (e.g. TOTAL_LAPS, GRID_SIZE, duel/solo lobby entries, collisionPairs) and re-anchoring the grid-geometry check to the new START coordinates. Every retained assertion must pass. Run all three tests with `node tests/<file> .` from the repo root and confirm they exit 0.
  </action>
  <verify>
    <automated>cd "c:/Users/Julio/OneDrive/Documents/GitHub/JuegoTest" && node tests/chase-cam-smoke.test.js . && node tests/r4a-smoke.test.js . && node tests/r4b-release.test.js .</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/chase-cam-smoke.test.js .` exits 0 and prints an OK summary; it asserts projectSegment/renderRoad/SEGMENTS/DRAW_DISTANCE/drawKartSprite present and drawTrack/drawMinimap/updateDRSUI/Monaco strings/btn-drs absent
    - `node tests/r4a-smoke.test.js .` and `node tests/r4b-release.test.js .` both exit 0 (obsolete Monaco/DRS/pixel-ground assertions removed or re-anchored; no false failures)
    - No retained assertion references a removed symbol or Monaco geometry
  </acceptance_criteria>
  <done>All three node smoke tests pass against the rewritten renderer, giving RENDER-01/RENDER-04 structural regression coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Phase RELEASE.md + ROADMAP.md + CLAUDE.md documentation</name>
  <read_first>
    - CLAUDE.md (full — "Project overview", "Architecture" sections describe the OLD top-down/Monaco pipeline that must be updated to the chase-cam renderer)
    - .planning/ROADMAP.md — Phase 5 section (Plans: TBD → finalize plan list)
    - .planning/phases/05-chase-cam-renderer-foundation/05-01-SUMMARY.md through 05-05-SUMMARY.md (scope actually delivered, files/assets, decisions applied)
    - An existing RELEASE.md from a prior phase for format (grep for RELEASE.md under .planning/phases or .planning/milestones)
  </read_first>
  <action>
    Write .planning/phases/05-chase-cam-renderer-foundation/05-RELEASE.md capturing, per the mandatory release documentation workflow: scope (chase-cam pseudo-3D renderer replacing top-down Monaco), the CONTEXT decisions applied (D-01..D-13, noting D-07 already reflected in REQUIREMENTS.md), files and assets changed (game.js sections, assets/kart-chase.png, index.html, style.css, tests/), compatibility constraints (multiplayer deliberately stale/hidden per D-04/D-05, retuned in Phase 10; physics constants untouched per D-03; pixel-tile road texture is throwaway per D-08 replaced in Phase 8), validation performed (three node smoke tests pass; mobile/visual sign-off gated in Plan 07), and remaining blockers (mobile-device profiling + curve+crest visual sign-off pending Plan 07's human checkpoint). Update .planning/ROADMAP.md Phase 5 section: set "Plans:" to the finalized list (05-01 through 05-07 with one-line objectives) and update the Progress table row for Phase 5 to reflect plan count. Update CLAUDE.md's "Project overview" and "Architecture" sections to describe the new chase-cam pseudo-3D road-segment renderer (SEGMENTS[] table, projectSegment/renderRoad scanline projector, chase camera, drawKartSprite frame-selected sprites, draw-distance culling) replacing the retired top-down/Monaco/buildEnvCanvas description — and note DRS and the minimap are removed and multiplayer is temporarily hidden. Do NOT overstate: mark mobile sign-off as pending Plan 07.
  </action>
  <verify>
    <automated>cd "c:/Users/Julio/OneDrive/Documents/GitHub/JuegoTest" && ls -la .planning/phases/05-chase-cam-renderer-foundation/05-RELEASE.md; grep -c "chase-cam\|SEGMENTS\|renderRoad\|projectSegment" CLAUDE.md; grep -c "05-01-PLAN\|05-02-PLAN" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/05-chase-cam-renderer-foundation/05-RELEASE.md exists and records scope, decisions (D-01..D-13), files/assets, compatibility constraints, validation, and the pending mobile sign-off blocker
    - CLAUDE.md no longer describes the top-down/Monaco renderer as current and mentions the chase-cam renderer (grep for chase-cam/SEGMENTS/renderRoad returns ≥1)
    - ROADMAP.md Phase 5 section lists the finalized plans (05-01..05-07) and the Progress table row is updated
    - Documentation flags mobile-device profiling as pending Plan 07 (not claimed complete)
  </acceptance_criteria>
  <done>The phase is documented per the project's mandatory workflow; ROADMAP and CLAUDE.md reflect the new architecture; the only open item is the Plan 07 human sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Adds test scripts and documentation only. The test scripts read files and run under node locally; they do not execute game.js or touch the network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | test scripts | accept | Tests are source-string assertions run locally via node; no code execution of game.js, no external input. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs; tests use only node built-ins (fs, assert), matching the existing test convention. |
</threat_model>

<verification>
- All three node smoke tests pass against the rewritten renderer.
- RELEASE.md, ROADMAP.md and CLAUDE.md updated in this change (mandatory workflow satisfied).
- Mobile sign-off documented as pending Plan 07.
</verification>

<success_criteria>
- RENDER-01/RENDER-04 have automated structural regression coverage.
- The mandatory per-phase release documentation exists and is accurate.
</success_criteria>

<output>
Create `.planning/phases/05-chase-cam-renderer-foundation/05-06-SUMMARY.md` when done.
</output>
