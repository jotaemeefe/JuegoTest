---
phase: 05-chase-cam-renderer-foundation
plan: 04
type: execute
wave: 4
depends_on: ["05-02", "05-03"]
files_modified: [game.js]
autonomous: true
requirements: [RENDER-02]

must_haves:
  truths:
    - "Segment scroll rate visibly increases with speed (the road rushes past faster when driving fast)"
    - "Rumble strips alternate/strobe and the strobe reads faster at higher speed"
    - "Screen shake and a FOV/vignette response scale up at high speed and settle at low speed"
    - "Engine audio pitch still scales with speed (not regressed from the top-down build)"
  artifacts:
    - path: "game.js"
      provides: "Speed-scaled camera lookahead/FOV, high-speed shake+vignette, verified engine-pitch hookup"
      contains: "speedFactor"
  key_links:
    - from: "cars[0].speed"
      to: "camera FOV / shake / vignette"
      via: "normalized speedFactor drives all speed cues"
      pattern: "speedFactor"
    - from: "cars[0].speed"
      to: "engine pitch"
      via: "updateEnginePitch retained in racing loop"
      pattern: "updateEnginePitch"
---

<objective>
Make the chase-cam renderer FEEL fast (RENDER-02): tie segment scroll rate, rumble-strip strobe, screen shake, a FOV/vignette response, and audio pitch to the player's speed so all the classic arcade speed cues scale visibly and audibly together.

Purpose: A correctly-projected road (Plan 02) plus a kart (Plan 03) can still feel inert. RENDER-02 is the requirement that turns "renders differently" into "feels fast" — the milestone's explicit problem statement is that the current handling/feel is "soso" (bland). This plan delivers the speed-sensation layer.
Output: Speed-driven camera/VFX/audio cues layered onto the existing renderer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-chase-cam-renderer-foundation/05-CONTEXT.md
@.planning/phases/05-chase-cam-renderer-foundation/05-UI-SPEC.md
@.planning/research/PITFALLS.md
@.planning/phases/05-chase-cam-renderer-foundation/05-02-SUMMARY.md

<interfaces>
<!-- Consumed from prior plans + existing codebase -->
- renderRoad(cameraZ, cameraX) , updateCamera(dt) , CAMERA_DEPTH , DRAW_DISTANCE (Plan 02)
- SEGMENTS[].color (rumble band flag) (Plan 01)
- updateEnginePitch(speed) : existing engine-pitch audio hook (game.js:473) — called at game.js:1972
- triggerShake() : existing screen-shake VFX (game.js:1357)
- MAX_SPD_ON = 450 : top speed, for normalizing speed to 0..1 (game.js:5)
- cars[0].speed : player speed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Speed-scaled scroll, rumble strobe and FOV response</name>
  <read_first>
    - game.js Plan 02's updateCamera/renderRoad (speed-scaled lookahead hook; CAMERA_DEPTH usage)
    - game.js line 5 (MAX_SPD_ON = 450 for normalization), lines 1791-1800 original camera lookahead pattern
    - .planning/phases/05-chase-cam-renderer-foundation/05-UI-SPEC.md — Color (accent gold #F4A93A rumble "on" stripe; speed-flash vignette tint), Canvas Render Contract (FOV/draw distance)
    - .planning/research/SUMMARY.md — "Speed sensation via segment scroll rate (distance-keyed rumble-strip strobe, FOV/shake response, audio pitch scaling)"
  </read_first>
  <action>
    Define a normalized speedFactor = clamp(cars[0].speed / MAX_SPD_ON, 0, 1) computed once per racing frame. Use it to drive three cues: (1) SCROLL — ensure the camera z advance / lookahead already scales with speed (Plan 02 ties cameraZ to player arc-length, so faster speed already scrolls segments faster; verify and, if needed, add a small speed-scaled lookahead so the horizon pulls forward at speed). (2) RUMBLE STROBE — the SEGMENTS[].color alternation already strobes as segments scroll; confirm the "on" rumble band uses the accent gold #F4A93A (UI-SPEC) and that the strobe reads faster at higher speed (it does, because scroll is faster) — no fixed-time animation, keep it distance-keyed so it stays coupled to speed. (3) FOV — widen the effective field of view slightly at high speed (nudge CAMERA_DEPTH or the horizon framing by a small function of speedFactor) so the world appears to rush outward when fast and calm when slow; clamp the effect so the road never distorts into the Pitfall 1 kink range. Keep all magnitudes subtle and tunable (exact values Claude's discretion per D-10/CONTEXT).
  </action>
  <verify>
    <automated>cd "c:/Users/Julio/OneDrive/Documents/GitHub/JuegoTest" && grep -nE "speedFactor" game.js | head; grep -c "F4A93A" game.js</automated>
  </verify>
  <acceptance_criteria>
    - A speedFactor (or equivalently-named normalized 0..1 speed value) is computed in the racing loop and referenced by the FOV/scroll cues
    - The rumble "on" band uses accent gold #F4A93A (grep count ≥1) and strobing is distance-keyed (coupled to scroll, not a fixed timer)
    - FOV/horizon widens as a function of speedFactor and is clamped so it never pushes projection into visible kinking
    - The segment scroll rate is driven by player speed (cameraZ advance tied to speed), not a constant
  </acceptance_criteria>
  <done>Driving fast makes the road visibly rush past faster with a faster rumble strobe and a subtly wider view; slowing down calms all three.</done>
</task>

<task type="auto">
  <name>Task 2: High-speed shake + vignette and retained engine-pitch scaling</name>
  <read_first>
    - game.js lines 1357 (triggerShake), 1750-1764 (drawSpeedLines — existing screen-space speed VFX to reference), 2072-2099 (screen-space render section where a vignette overlay belongs)
    - game.js lines 473-500 (updateEnginePitch), 1971-1985 (racing-loop audio call sites — must remain intact)
    - .planning/research/PITFALLS.md — Pitfall 9 (do not regress lazy AudioContext/audio hooks during a render rewrite)
    - .planning/phases/05-chase-cam-renderer-foundation/05-UI-SPEC.md — Color (speed-flash/shake vignette tint uses accent gold at high speed)
  </read_first>
  <action>
    Add a high-speed shake+vignette response in the screen-space render section (after ctx.restore, around game.js:2072): when speedFactor exceeds a threshold (e.g. > 0.75), apply a subtle continuous screen shake (reuse the existing triggerShake / shake-offset mechanism or an equivalent small per-frame jitter scaled by speedFactor) and draw an edge vignette whose intensity scales with speedFactor, tinted with the accent gold #F4A93A at the top end (UI-SPEC speed-flash). Keep it below the wrong-way and damage overlays in draw order and subtle enough not to obscure the HUD. Separately, VERIFY (do not remove) that updateEnginePitch(cars[0].speed) is still called every racing frame (game.js:1972) so engine pitch continues to scale with speed — this is the audio leg of RENDER-02 and must not have regressed during Plans 02-03; if the render-block rewrite displaced it, restore the call. Do NOT create a new AudioContext — engine pitch already routes through the lazy getAudioCtx singleton (Pitfall 9).
  </action>
  <verify>
    <automated>cd "c:/Users/Julio/OneDrive/Documents/GitHub/JuegoTest" && grep -c "updateEnginePitch(cars" game.js; grep -nE "vignette|triggerShake|shakeMag" game.js | head; grep -c "new (window.AudioContext" game.js</automated>
  </verify>
  <acceptance_criteria>
    - A vignette/shake response scaled by speedFactor exists in the screen-space render section and is drawn below HUD/wrong-way/damage overlays
    - `grep -c "updateEnginePitch(cars"` returns ≥1 (engine pitch still hooked to player speed — not regressed)
    - `grep -c "new (window.AudioContext"` returns exactly 1 (only the existing lazy getAudioCtx singleton; no second context created — Pitfall 9)
    - The high-speed effect is gated by a speed threshold so it is absent/subtle at low speed
  </acceptance_criteria>
  <done>At high speed the frame shakes subtly with a gold-tinted edge vignette and rising engine pitch; at low speed the frame is calm and the HUD stays legible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Speed cues are derived from local player speed and drive only rendering and the existing audio synth. No user input parsing, no network, no new AudioContext, no DOM/eval sink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Denial of Service | per-frame vignette/shake overlay | accept | Constant-cost screen-space overlay drawn once per frame; negligible and bounded. |
| T-05-A | Tampering | audio subsystem | mitigate | New sound cues (none added here) and pitch scaling route only through the existing lazy getAudioCtx singleton; verify no second AudioContext is created (Pitfall 9). |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs; zero-dependency project. |
</threat_model>

<verification>
- All four RENDER-02 speed cues (scroll, rumble strobe, shake/FOV, audio pitch) scale with player speed.
- No second AudioContext is created; engine-pitch hook retained (Pitfall 9).
- High-speed vignette/shake is subtle and never obscures the HUD.
</verification>

<success_criteria>
- RENDER-02: speed sensation is present and visibly/audibly scales with speed — the ROADMAP Phase 5 success criterion 3.
</success_criteria>

<output>
Create `.planning/phases/05-chase-cam-renderer-foundation/05-04-SUMMARY.md` when done.
</output>
