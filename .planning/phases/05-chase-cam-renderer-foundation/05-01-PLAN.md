---
phase: 05-chase-cam-renderer-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [game.js]
autonomous: true
requirements: [RENDER-01, RENDER-03]

must_haves:
  truths:
    - "The Monaco ROAD_SPINE / Beau Rivage / Swimming Pool centerline is gone; a new synthetic closed-oval test loop is the only track"
    - "A dense SEGMENTS[] table exists carrying per-segment curve, elevation, width and color for the projector to walk"
    - "At least one region of the loop has a curve and an elevation crest coinciding at the same arc-length (the RENDER-03 test-case data)"
    - "Existing world-space physics (isOnTrack/nearestSpinePoint/updateAI/wrong-way/crossedFinish) keep working against the new loop with no constant retuning"
  artifacts:
    - path: "game.js"
      provides: "New synthetic closed-loop ROAD_SPINE, AI_WAYPOINTS, CPS, START, FINISH, plus SEGMENTS[] dense table and segmentAt(z) helper"
      contains: "const SEGMENTS"
  key_links:
    - from: "SEGMENTS[]"
      to: "ROAD_SPINE arc-length"
      via: "uniform resample keyed on SPINE_CUMLEN"
      pattern: "SEGMENTS"
    - from: "crossedFinish"
      to: "new start straight"
      via: "FINISH_X east-heading segment test unchanged"
      pattern: "crossedFinish"
---

<objective>
Retire the Monaco top-down track and author the brand-new synthetic closed-loop test track this phase's renderer will draw, in two representations: (1) the existing world-space centerline (ROAD_SPINE, reused by unchanged physics per D-03) re-authored as a believable drivable oval, and (2) a NEW dense, uniformly-spaced SEGMENTS[] table carrying curve / elevation / width / color per segment for the scanline projector (built in Plan 02).

Purpose: Nothing else in the phase can be built or tested without a track. Per research build order, the segment table on a closed test loop is step 1 (unblocks projector, physics bounds, camera z). The loop must include the deliberate curve+crest coincidence (RENDER-03 / PITFALLS.md Pitfall 1) so the projector has the exact case it must render without kinking.
Output: A self-contained track-data section in game.js that replaces Monaco and adds SEGMENTS[].
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-chase-cam-renderer-foundation/05-CONTEXT.md
@.planning/phases/05-chase-cam-renderer-foundation/05-PATTERNS.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Monaco centerline with a synthetic closed-oval test loop</name>
  <read_first>
    - game.js lines 1-108 (constants, WORLD_SCALE/worldPoint, ROAD_HALF_W, ROAD_SPINE, SPINE_CUMLEN, FINISH_ARC, START grid)
    - game.js lines 203-265 (AI_WAYPOINTS and CPS gate definitions — grep "CPS" and "AI_WAYPOINTS" for exact ranges)
    - game.js line 672 (TUNNEL_ZONE), lines 1198-1220 (trackProgress/crossedFinish; 1214 east-heading finish test)
    - .planning/phases/05-chase-cam-renderer-foundation/05-CONTEXT.md — D-02 (retire Monaco, synthetic track), D-03 (physics unchanged)
    - .planning/phases/05-chase-cam-renderer-foundation/05-PATTERNS.md — "Synthetic test-loop SEGMENTS[] table" section (authoring convention, prefix-sum IIFE, finish-arc-from-geometry pattern)
  </read_first>
  <action>
    Replace the contents (not the variable name) of ROAD_SPINE (game.js:35-83) with a new closed-oval centerline authored in the same worldPoint-mapped [x,y] convention, comment-blocked by named track section, forming a believable drivable loop with at least two curved corners. Author a straight start/finish leg running EAST (constant y, increasing x) so crossedFinish's existing east-heading segment-crossing test (game.js:1214, requires prevX < FINISH_X <= x and cos(angle) > 0) still holds unchanged. Keep ROAD_HALF_W as the physics half-width. Re-author AI_WAYPOINTS (game.js:203) to trace the new centerline at the same ~55-point density. Re-anchor the CPS[1..3] anti-shortcut gates onto three well-separated points around the new loop (hit-in-order semantics unchanged). Re-anchor FINISH_X / FINISH_Y (game.js:94) onto the new start straight and keep the SPINE_CUMLEN / SPINE_TOTAL_LEN / FINISH_ARC IIFE-prefix-sum pattern (game.js:84-99) unchanged in shape (derive FINISH_ARC from nearestSpinePoint, do not hardcode). Re-anchor the START grid array (game.js:103) onto the start straight behind the finish line, preserving the 11-row × 2-wide staggered formula (30px row spacing, lanes at ±(ROAD_HALF_W/2)) so the 22-car grid has no overlap. Neutralize the Monaco tunnel: redefine TUNNEL_ZONE (game.js:672) to a zero-area / out-of-world box so the per-frame car.inTunnel test (game.js:1982) always evaluates false on the test loop. Do NOT modify ACCEL_RATE, TURN_RATE, MAX_SPD_ON, MAX_SPD_OFF, BRAKE_FORCE, GRIP_ON, GRIP_OFF (D-03 defers handling changes to Phase 6). Do NOT rename ROAD_SPINE — isOnTrack/nearestSpinePoint/updateAI/wrong-way read it and must keep working.
  </action>
  <verify>
    <automated>cd "c:/Users/Julio/OneDrive/Documents/GitHub/JuegoTest" && grep -c "Beau Rivage\|Swimming Pool\|Monte Carlo" game.js; grep -nE "const ROAD_SPINE|const AI_WAYPOINTS|const SPINE_CUMLEN|FINISH_ARC" game.js; grep -nE "ACCEL_RATE|TURN_RATE|MAX_SPD_ON" game.js | grep "const"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Beau Rivage\|Swimming Pool\|Monte Carlo"` on game.js returns 0 (Monaco centerline comments removed)
    - ROAD_SPINE, AI_WAYPOINTS, SPINE_CUMLEN and FINISH_ARC still exist as declarations (physics contract preserved)
    - ACCEL_RATE (2.0), TURN_RATE (4.5), MAX_SPD_ON (450) constant lines are byte-identical to before (D-03)
    - The START array still produces 22 entries with no pair closer than 2×CAR_RADIUS (staggered grid re-anchored)
    - crossedFinish (game.js:1214) is unchanged and its east-heading assumption is satisfied by the new start straight
    - TUNNEL_ZONE is redefined so no world position can satisfy the inTunnel range test (car.inTunnel always false)
  </acceptance_criteria>
  <done>Starting VS CPU no longer references Monaco geometry; cars spawn on the new loop's start straight and the existing physics/AI drive the new centerline without console errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build the dense SEGMENTS[] table with curve, elevation, width, color</name>
  <read_first>
    - game.js lines 84-99 (SPINE_CUMLEN prefix-sum IIFE, SPINE_TOTAL_LEN — the pattern to mirror for segment cumulative length)
    - .planning/research/ARCHITECTURE.md — Pattern 2 "Segment table + scanline projector" (curve/y/width/color per uniformly-spaced entry; SEGMENT_LENGTH spacing)
    - .planning/research/PITFALLS.md — Pitfall 1 (curve and elevation as two independent per-segment accumulators; deliberate corner-on-crest test case)
    - .planning/phases/05-chase-cam-renderer-foundation/05-UI-SPEC.md — Color section (asphalt #5C534A, cream kerb #E4E0D6, accent gold #F4A93A rumble "on" stripe) and Test loop requirements
    - .planning/phases/05-chase-cam-renderer-foundation/05-PATTERNS.md — "RENDER-03 requirement reminder"
  </read_first>
  <action>
    Add a new track-data section immediately after the ROAD_SPINE/SPINE_CUMLEN block. Define SEGMENT_LENGTH (uniform world-unit spacing, default ~200 world units) and build a SEGMENTS[] array by resampling the new ROAD_SPINE at uniform SEGMENT_LENGTH steps along arc-length (using SPINE_CUMLEN for the arc→point mapping). Each segment object must carry: index, z (index × SEGMENT_LENGTH), curve (signed, negative=left / positive=right, derived from the change in spine heading across the segment and eased between neighbors so corners ramp in/out rather than step), y (elevation — a NEW purely-visual channel not present in ROAD_SPINE; author a smooth hill profile with at least one crest), width (per-segment road half-width, default ROAD_HALF_W), and color (a two-state band flag such as index%2, for rumble/kerb striping). Position the elevation crest so it coincides in arc-length with one of the loop's curved corners — this is the deliberate RENDER-03 / Pitfall 1 curve+crest test case; add a code comment `RENDER-03 curve+crest test case` on that segment range. Keep curve and y as independent fields (do NOT pre-combine them; Plan 02's projector folds both into one perspective divide). Add SEG_TOTAL_LEN (analogous to SPINE_TOTAL_LEN) and a segmentAt(z) helper returning the segment for a camera z, wrapping the closed loop via modulo of the total length. Add a color palette constant using the UI-SPEC Color tokens (asphalt, kerb, rumble-on gold) as solid-color fallbacks so the projector can render before textures load.
  </action>
  <verify>
    <automated>cd "c:/Users/Julio/OneDrive/Documents/GitHub/JuegoTest" && grep -nE "const SEGMENTS|const SEGMENT_LENGTH|function segmentAt|SEG_TOTAL_LEN" game.js; grep -c "RENDER-03 curve" game.js; grep -nE "curve\s*:" game.js | head -3</automated>
  </verify>
  <acceptance_criteria>
    - SEGMENTS, SEGMENT_LENGTH, segmentAt and SEG_TOTAL_LEN are all declared in game.js
    - `grep -c "RENDER-03 curve"` returns at least 1 (the deliberate curve+crest test-case segment is marked)
    - Each SEGMENTS entry object literal contains curve, y, width and color fields (independent curve and elevation channels)
    - segmentAt(z) wraps correctly for z beyond SEG_TOTAL_LEN (closed-loop modulo), verifiable by inspecting the modulo in the helper body
    - A color-token fallback constant (asphalt #5C534A / kerb #E4E0D6 / rumble gold #F4A93A) exists for pre-texture rendering
  </acceptance_criteria>
  <done>SEGMENTS[] is populated at load with uniformly-spaced entries covering the whole loop, curve and elevation vary smoothly, and one segment range has both a nonzero curve and a crest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan only edits in-repo static track data (arrays/constants). No user input, no network, no DOM injection, no file/eval boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | ROAD_SPINE / SEGMENTS static data | accept | Client-side hardcoded arrays; a modified local client only affects the cheater's own single-player session (multiplayer is stale/hidden this phase per D-04/D-05). No shared state, no server. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; project has zero dependencies and no package.json. RESEARCH.md confirms "add no new dependencies." |
</threat_model>

<verification>
- Monaco geometry and comments are fully removed from game.js (grep for Monaco landmark names returns 0).
- The new ROAD_SPINE forms a closed drivable oval with an east-running start straight; physics constants untouched (D-03).
- SEGMENTS[] exists with curve/elevation/width/color per entry and a marked curve+crest test-case segment (RENDER-03 data).
- segmentAt(z) resolves and wraps around the closed loop.
</verification>

<success_criteria>
- The game boots into VS CPU on the new synthetic loop with no console errors.
- SEGMENTS[] and its helpers are ready for Plan 02's projector to consume.
- All existing physics/AI functions operate against the new centerline unchanged.
</success_criteria>

<output>
Create `.planning/phases/05-chase-cam-renderer-foundation/05-01-SUMMARY.md` when done.
</output>
