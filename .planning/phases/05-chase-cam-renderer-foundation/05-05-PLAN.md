---
phase: 05-chase-cam-renderer-foundation
plan: 05
type: execute
wave: 5
depends_on: ["05-02", "05-04"]
files_modified: [game.js, index.html, style.css]
autonomous: true
requirements: [RENDER-01]

must_haves:
  truths:
    - "The lap counter, race timer, position badge and role/gap indicator are adapted in place onto the new chase-cam view and read legibly"
    - "The HUD typography/color has begun moving toward the Silksong direction (system-ui bold sans, gold #F4A93A accent)"
    - "The minimap is gone this phase"
    - "The DRS button and all DRS HUD/logic call sites are removed (DRS is out of scope for v3.0)"
    - "Multiplayer create/join entries are hidden from the lobby; VS CPU modes remain visible"
    - "The countdown overlay names the test loop, not Monaco"
  artifacts:
    - path: "game.js"
      provides: "DRS/minimap removal, countdown/win copy swap, HUD restyle wiring"
    - path: "index.html"
      provides: "DRS button removed, lobby MP entries hidden, tagline updated"
    - path: "style.css"
      provides: "HUD typography/color moved toward Silksong direction"
  key_links:
    - from: "lobby MP buttons"
      to: "hidden"
      via: "D-05 scope-hide for Phases 5-9"
      pattern: "btn-create"
    - from: "DRS call sites"
      to: "removed"
      via: "REQUIREMENTS.md DRS out of scope"
      pattern: "updateDRSUI"
---

<objective>
Complete the "first playable version feels like a real game" requirement: adapt the existing HUD onto the new chase-cam view with a light restyle toward the new art direction (D-11/D-13), remove the elements that no longer belong this phase (minimap D-12, DRS which is out of scope for the whole milestone), hide the broken multiplayer entry points (D-05), and swap the retired Monaco copy for the test-loop name (D-02).

Purpose: These are the UI-chrome deliverables that make the chase-cam build read as a game rather than a tech demo, and they remove dead/stale surfaces (DRS, minimap, multiplayer) before they visually contradict the new direction.
Output: HUD/lobby/overlay changes across game.js, index.html, style.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-chase-cam-renderer-foundation/05-CONTEXT.md
@.planning/phases/05-chase-cam-renderer-foundation/05-PATTERNS.md
@.planning/phases/05-chase-cam-renderer-foundation/05-UI-SPEC.md
@.planning/phases/05-chase-cam-renderer-foundation/05-02-SUMMARY.md

<interfaces>
<!-- Existing DOM/CSS/JS surfaces this plan edits -->
- HUD DOM refs: hudLap, hudTimer, hudPos, hudRole (game.js:282-285); markup index.html:70-75
- updateHUD() (game.js:1625-1688) — same logic, restyled
- drawCountdown()/drawWin() (game.js:1691-1732) — copy swap
- drawMinimap() (game.js:1803), call sites game.js:2074/2124/2149
- DRS: isInDrsZone/useDRS (game.js:1340-1355), updateDRSUI (1766), drawDRSStatus (1776), call sites 1988/2044/2077; #btn-drs (index.html:78); .drs-btn CSS (style.css:301-331); boostMul in updateCar/updateAI
- Lobby MP entries: #btn-create, #btn-join-screen, .lobby-divider (index.html:24-26); tagline .tagline (index.html:18)
- CSS: .game-hud/.hud-item/.hud-pos/.hud-timer (style.css:251-276)
- Color tokens: accent gold #F4A93A (UI-SPEC)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HUD restyle + countdown/win copy swap + lobby tagline</name>
  <read_first>
    - game.js lines 1625-1688 (updateHUD logic — keep, do not rebuild), 1691-1732 (drawCountdown/drawWin — copy targets)
    - style.css lines 251-276 (.game-hud/.hud-item/.hud-pos/.hud-timer)
    - index.html lines 15-27 (logo-block, tagline, lobby buttons), 70-75 (HUD markup)
    - .planning/phases/05-chase-cam-renderer-foundation/05-UI-SPEC.md — Typography (Label 12/700, Body 16/700, system-ui bold sans), Color (accent gold #F4A93A for active-state numerals), Copywriting Contract (test-loop name, tagline)
    - .planning/phases/05-chase-cam-renderer-foundation/05-CONTEXT.md — D-11 (adapt in place), D-13 (begin moving HUD style), D-02 (retire Monaco copy)
  </read_first>
  <action>
    In style.css (.hud-item and the .game-hud group, lines 251-276): add an explicit font-family override `system-ui, -apple-system, 'Segoe UI', sans-serif` (currently the HUD inherits the page monospace default), bump the Label role (hud-role) to 12px/700 and the Body role (hud-lap, hud-pos, hud-timer) to 16px/700 per UI-SPEC Typography, and swap the accent color from #fbbf24 to the new gold token #F4A93A on .hud-pos and .hud-timer.record. Keep the legacy `7px 14px` padding and the 68px touch-button height exactly as-is (UI-SPEC grandfathers these). Do NOT change updateHUD()'s logic (game.js:1625-1688) — it is adapted in place (D-11), only restyled. In game.js drawCountdown (game.js ~1701/1704): replace the two hardcoded strings 'CIRCUIT DE MONACO' and 'MONTE CARLO · MÓNACO' with title line 'KARTÓDROMO PILOTO' and subtitle 'CIRCUITO DE PRUEBA' (UI-SPEC Copywriting) — keep the draw-call structure and fonts unchanged. In index.html line 18, replace the tagline 'R4A GRAND PRIX · 22 AUTOS · PIXEL RACING' with 'ARCADE REBIRTH · KARTÓDROMO' (the "PIXEL RACING" claim contradicts the new illustrated view). Leave drawWin() copy (¡GANASTE!/¡Buen intento!) unchanged — no decision touches it.
  </action>
  <verify>
    <automated>cd "c:/Users/Julio/OneDrive/Documents/GitHub/JuegoTest" && grep -c "CIRCUIT DE MONACO\|MONTE CARLO" game.js; grep -c "KARTÓDROMO PILOTO\|CIRCUITO DE PRUEBA" game.js; grep -c "PIXEL RACING" index.html; grep -c "F4A93A" style.css; grep -c "system-ui" style.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CIRCUIT DE MONACO\|MONTE CARLO"` on game.js returns 0; `grep -c "KARTÓDROMO PILOTO\|CIRCUITO DE PRUEBA"` returns ≥2 (copy swapped, D-02)
    - `grep -c "PIXEL RACING"` on index.html returns 0 (tagline updated)
    - style.css .hud group has an explicit system-ui font-family and uses #F4A93A for the active-state numerals
    - updateHUD() logic in game.js is unchanged (same lap/pos/gap computation, D-11)
    - HUD Body-role sizing is 16px/700 and Label-role 12px/700 (UI-SPEC Typography)
  </acceptance_criteria>
  <done>The HUD renders in bold system-ui with gold active-state numerals over the chase-cam view; the countdown names the test loop; the lobby tagline no longer says "PIXEL RACING".</done>
</task>

<task type="auto">
  <name>Task 2: Remove DRS and minimap; hide lobby multiplayer entries</name>
  <read_first>
    - game.js lines 1340-1355 (isInDrsZone/useDRS), 1766-1801 (updateDRSUI/drawDRSStatus), 1803-1850 (drawMinimap), and DRS/minimap call sites at 1988, 2044, 2074, 2077, 2124, 2149 (grep "updateDRSUI\|drawDRSStatus\|drawMinimap\|useDRS\|isInDrsZone" for the full set)
    - game.js updateCar/updateAI boostMul factor (grep "boostMul" and "useDRS(" — the DRS speed multiplier to strip)
    - index.html lines 24-26 (lobby MP buttons + divider), line 78 (#btn-drs)
    - style.css lines 301-331 (.drs-btn — may be left dormant or removed)
    - .planning/phases/05-chase-cam-renderer-foundation/05-CONTEXT.md — D-05 (hide MP entries), D-12 (remove minimap); .planning/phases/.../05-UI-SPEC.md — DRS removal note; .planning/phases/.../05-PATTERNS.md — "Minimap + DRS UI removal", "Element-hide via hidden property"
    - .planning/REQUIREMENTS.md — Out of Scope table (DRS out of scope for v3.0)
  </read_first>
  <action>
    Remove DRS entirely: delete the drawMinimap() call sites (game.js:2074, 2124, 2149) and the drawMinimap function body (game.js:1803-1850) per D-12; delete the updateDRSUI() and drawDRSStatus() call sites (game.js:1988, 2044, 2077) and their function bodies (1766-1801); strip the DRS boost multiplier from updateCar and updateAI (remove the `useDRS(car, keys.boost)` call and the `boostMul` factor from the maxSpd computation so maxSpd no longer includes a DRS term — see PATTERNS.md physics section) and remove isInDrsZone/useDRS (1340-1355) and any DRS-related car fields' HUD usage; remove the #btn-drs element from index.html line 78 and its .drs-btn CSS (style.css:301-331). In index.html, hide the multiplayer lobby entries per D-05: add the `hidden` attribute to #btn-create (line 25), #btn-join-screen (line 26) and the .lobby-divider (line 24) — keep #btn-solo and #btn-duel visible. Do NOT delete the create/join screens or the Net IIFE (D-04: multiplayer stays stale, just hidden — it returns in Phase 10). Verify no remaining code references a now-deleted DRS/minimap symbol (no ReferenceError on load).
  </action>
  <verify>
    <automated>cd "c:/Users/Julio/OneDrive/Documents/GitHub/JuegoTest" && grep -c "drawMinimap\|updateDRSUI\|drawDRSStatus\|isInDrsZone\|useDRS\|boostMul" game.js; grep -c "btn-drs" index.html; grep -nE "btn-create|btn-join-screen|lobby-divider" index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "drawMinimap\|updateDRSUI\|drawDRSStatus\|isInDrsZone\|useDRS\|boostMul"` on game.js returns 0 (all DRS + minimap symbols removed, no dangling call sites)
    - `grep -c "btn-drs"` on index.html returns 0 (DRS button removed)
    - #btn-create, #btn-join-screen and .lobby-divider each carry the `hidden` attribute in index.html (D-05)
    - #btn-solo and #btn-duel remain visible (not hidden)
    - The Net IIFE and create/join screen markup still exist (D-04 — multiplayer only hidden, not deleted)
    - The game loads with no ReferenceError (no code calls a deleted symbol)
  </acceptance_criteria>
  <done>The chase-cam HUD shows no minimap and no DRS button; maxSpd has no DRS term; the lobby shows only Grand Prix and Duel; multiplayer code remains dormant for Phase 10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Edits DOM chrome, CSS and HUD/overlay draw code. Hiding (not deleting) the multiplayer entry buttons reduces exposed surface. No new input parsing, no network payload change, no eval/innerHTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Elevation of Privilege | hidden multiplayer entry | accept | Buttons are hidden via the `hidden` attribute; the underlying stale Net path is unreachable through normal UI. Multiplayer is knowingly non-functional this phase (D-04) and re-validated in Phase 10 before re-exposure. A user un-hiding the button locally only reaches the same stale path, affecting only their own session. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs; zero-dependency project. |
</threat_model>

<verification>
- HUD adapted in place with restyled typography/color (D-11/D-13); minimap removed (D-12); DRS fully removed (out of scope); MP entries hidden (D-05); Monaco copy swapped (D-02).
- No dangling references to removed symbols; multiplayer code preserved but dormant (D-04).
</verification>

<success_criteria>
- RENDER-01: the chase-cam view is presented as a complete first-playable game surface — HUD legible, no stale minimap/DRS/multiplayer chrome.
</success_criteria>

<output>
Create `.planning/phases/05-chase-cam-renderer-foundation/05-05-SUMMARY.md` when done.
</output>
