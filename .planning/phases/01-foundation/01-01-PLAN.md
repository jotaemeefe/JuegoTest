---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - game.js
autonomous: true
requirements:
  - BUG-01
  - BUG-02
  - BUG-03
  - BUG-04
  - CTRL-01
  - CTRL-02
  - CTRL-03
  - GRID-01
  - GRID-02

must_haves:
  truths:
    - "Clicking REVANCHA in solo mode restarts cleanly with no double audio/state glitch"
    - "Spacebar key applies brake force identically to ArrowDown and S keys"
    - "Arrow keys and WASD already confirmed working — no regression introduced"
    - "RIVALS array contains exactly 21 entries: all 2026 F1 drivers except Colapinto"
    - "RIVALS entries have correct 2026 team names, livery colors, car numbers, and skill tiers"
    - "cpScore lambda defined exactly once in updateHUD(); HUD compiles with no duplicate-const error"
    - "Lap 1 time is recorded and displayed correctly; lapStartTime initializes before CP0 first crossing"
  artifacts:
    - path: "game.js"
      provides: "Fixed solo restart, finish guard, single cpScore, spacebar brake, 21-entry RIVALS array"
      contains: "if (e.key === ' ') keys.down"
    - path: "game.js"
      provides: "2026 grid data"
      contains: "Cadillac"
  key_links:
    - from: "btn-restart handler (game.js ~1212)"
      to: "beginCountdown()"
      via: "solo branch calls beginCountdown() only — no preceding resetGame()"
      pattern: "} else \\{\\s*beginCountdown"
    - from: "onMsg finish handler (game.js ~992)"
      to: "remote.lap guard"
      via: "if (remote.lap < TOTAL_LAPS - 1) return — present for ALL receive paths"
      pattern: "remote\\.lap < TOTAL_LAPS"
    - from: "updateHUD (game.js ~673)"
      to: "cpScore lambda"
      via: "single const cpScore definition at top of updateHUD body"
      pattern: "const cpScore"
---

<objective>
Fix all four known bugs in game.js, add spacebar-as-brake control, and replace the RIVALS array with the correct 2026 F1 grid (21 rivals). All changes are surgical: targeted line edits and a data-only RIVALS replacement. No architectural changes.

Purpose: Zero known bugs + correct 2026 grid = the foundation all future phases build on.
Output: Modified game.js with 4 bug fixes, CTRL-03 addition, and 21-entry 2026 RIVALS array.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Key signatures extracted from game.js for executor reference. -->
<!-- Read game.js before editing — do not rely solely on these excerpts. -->

game.js:1053–1062 — keydown/keyup listeners (CTRL-03 target):
  window.addEventListener('keydown', e => {
    if (e.key === 'ArrowLeft'  || e.key.toLowerCase() === 'a') keys.left  = true;
    if (e.key === 'ArrowRight' || e.key.toLowerCase() === 'd') keys.right = true;
    if (e.key === 'ArrowDown'  || e.key.toLowerCase() === 's') keys.down  = true;
    // ADD: if (e.key === ' ') { e.preventDefault(); keys.down = true; }
  });
  window.addEventListener('keyup', e => {
    if (e.key === 'ArrowLeft'  || e.key.toLowerCase() === 'a') keys.left  = false;
    if (e.key === 'ArrowRight' || e.key.toLowerCase() === 'd') keys.right = false;
    if (e.key === 'ArrowDown'  || e.key.toLowerCase() === 's') keys.down  = false;
    // ADD: if (e.key === ' ') keys.down = false;
  });

game.js:673 — first cpScore definition (BUG-03 target):
  const cpScore = c => c.finished ? Infinity : c.lap * CPS.length + (c.nextCP === 0 ? CPS.length : c.nextCP);
  const isFirst = cpScore(local) >= cpScore(remote);

game.js:681–682 — second cpScore definition inside solo block (DUPLICATE — DELETE this one):
  const cpScore  = c => c.finished ? Infinity : c.lap * CPS.length + (c.nextCP === 0 ? CPS.length : c.nextCP);

game.js:1202–1213 — btn-restart handler (BUG-01 target):
  } else {
    resetGame(); beginCountdown();   // resetGame() here is redundant — DELETE resetGame()
  }

game.js:992–997 — finish message handler in onMsg (BUG-02 target):
  if (data.type === 'finish' && !winner) {
    if (remote.lap < TOTAL_LAPS - 1) return;   // VERIFY this guard exists for both host & guest paths
    winner = 'remote';
    phase  = 'done';
  }

game.js:1014–1043 — resetGame():
  lapStartTime = 0;   // set to 0 — OK; line 820 sets it to performance.now() at countdown→racing

game.js:819–820 — phase transition countdown→racing (BUG-04 verification target):
  phase = 'racing';
  lapStartTime = performance.now();   // CONFIRM this line exists; if not, add it here

game.js:58–83 — RIVALS array (GRID-01/02 target):
  const RIVALS = [
    { name, team, num, body, accent, helmet, skill }   // 20 entries currently; must become 21
  ];
  // Remove: Franco Colapinto (index 6), Yuki Tsunoda (index 11)
  // Add: Arvid Lindblad (Racing Bulls), Valtteri Bottas (Cadillac), Sergio Pérez (Cadillac)
  // Update team name: Bortoleto + Hülkenberg from 'Kick Sauber' → 'Audi'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bug fixes — BUG-01, BUG-02, BUG-03, BUG-04 + CTRL-03 spacebar</name>
  <files>game.js</files>
  <read_first>
    - game.js lines 668–706 (updateHUD — cpScore duplication, BUG-03)
    - game.js lines 816–825 (phase transition, BUG-04 verification)
    - game.js lines 990–1012 (onMsg finish handler, BUG-02)
    - game.js lines 1044–1062 (beginCountdown + keydown/keyup listeners, CTRL-03)
    - game.js lines 1200–1215 (btn-restart handler, BUG-01)
  </read_first>
  <action>
    Apply these five targeted edits to game.js. Read each section before editing.

    BUG-01 (line ~1212, solo branch of btn-restart handler, per D-11):
    Find the else branch of the btn-restart handler:
      } else {
        resetGame(); beginCountdown();
      }
    Change to:
      } else {
        beginCountdown();
      }
    Rationale: beginCountdown() calls resetGame() at line 1046. Calling resetGame() first causes double audio init and state overwrite.

    BUG-02 (lines ~992–997, onMsg finish handler, per D-12):
    Read the full onMsg function to find ALL code paths that handle data.type === 'finish'. The guard `if (remote.lap < TOTAL_LAPS - 1) return;` must be present BEFORE `winner = 'remote'` is set. Verify the guard is present at line 994. If it exists and covers the only finish-handler code path, BUG-02 is already fixed — note this in comments. If a second path exists without the guard, add the guard there. Do NOT remove or modify the existing guard if it is correct.

    BUG-03 (lines ~673 and ~682, updateHUD cpScore duplication, per D-13):
    In updateHUD(), confirm there are two const cpScore definitions. Delete the SECOND definition (inside the `if (gameMode === 'solo' && selectedRival)` block at ~line 682). The first definition at line 673 already covers both usages. The two usages at lines 674 and 683 both refer to the same lambda — after removal, line 683's usage (myP = cpScore(local)) will correctly reference the hoisted first definition.

    BUG-04 (line ~820, phase transition, per D-14):
    Read lines 816–825. Confirm that `lapStartTime = performance.now();` is set at the countdown→racing transition (inside `if (countdown < 0)` block). If it exists exactly as described, BUG-04 is verified-already-fixed — add a comment `// BUG-04: lapStartTime correctly initialized here (verified)` and make no further change. If the line is absent, add `lapStartTime = performance.now();` immediately after `phase = 'racing';`.

    CTRL-03 (lines ~1053–1062, keydown/keyup listeners, per D-06):
    In the keydown listener, after the existing three if-statements, add:
      if (e.key === ' ') { e.preventDefault(); keys.down = true; }
    In the keyup listener, after the existing three if-statements, add:
      if (e.key === ' ') keys.down = false;
    The e.preventDefault() on keydown prevents browser page-scroll when spacebar is pressed during gameplay. Do not add preventDefault to keyup — it is not needed there.

    CTRL-01 and CTRL-02 are already implemented (verified: ArrowLeft/Right/Down and a/d/s at lines 1054–1061). Do not touch those lines.
  </action>
  <verify>
    Open index.html in a browser. In the game screen:
    - Press spacebar: car brakes (speed drops). Release: car accelerates again.
    - Press spacebar in lobby/results: page does NOT scroll (e.preventDefault works).
    - Complete a solo race: clicking REVANCHA starts cleanly with no duplicate game-reset glitch (audio fires once).
    Also verify in browser console: no "Identifier 'cpScore' has already been declared" SyntaxError on page load.
  </verify>
  <acceptance_criteria>
    - game.js btn-restart solo branch contains exactly "beginCountdown();" with no preceding "resetGame();" on the same line or immediately before it within that branch
    - game.js updateHUD() contains exactly one occurrence of "const cpScore" (grep -c "const cpScore" game.js equals 1)
    - game.js keydown listener contains the string "e.key === ' '" with e.preventDefault() call
    - game.js keyup listener contains "e.key === ' '" setting keys.down = false
    - game.js line ~820 area contains "lapStartTime = performance.now();" within the countdown→racing transition block
    - Browser console shows zero SyntaxErrors on page load
  </acceptance_criteria>
  <done>Four bugs addressed (BUG-01 fixed, BUG-02 verified/fixed, BUG-03 fixed, BUG-04 verified), CTRL-03 spacebar brake functional, no console errors on load.</done>
</task>

<task type="auto">
  <name>Task 2: 2026 F1 Grid — replace RIVALS array (GRID-01, GRID-02)</name>
  <files>game.js</files>
  <read_first>
    - game.js lines 58–83 (current RIVALS array — all 20 entries)
    - game.js lines 1143–1183 (buildRivalGrid — references RIVALS by index)
    - https://www.formula1.com/en/drivers (canonical 2026 driver list, car numbers)
    - https://www.formula1.com/en/teams (team livery colors 2026)
  </read_first>
  <action>
    Replace the entire RIVALS array (lines 58–83 in game.js) with an updated 21-entry array for the 2026 F1 season, per D-01 through D-05.

    Before writing the array, browse formula1.com/en/drivers and formula1.com/en/teams to verify:
    - Exact car numbers for all drivers (especially Hadjar, Antonelli, Lindblad, Bottas, Pérez, Bearman)
    - Cadillac livery colors (body, accent) — no historical data exists in the game
    - Audi livery colors (updated from Kick Sauber green)
    - Whether Tsunoda has a 2026 seat (D-02 does not list him; verify before removing)
    - Verify Gasly is listed as Alpine (not a different team)

    Grid composition per D-02 (21 rivals = all 2026 drivers except Colapinto who is the player):
    McLaren: Norris, Piastri
    Ferrari: Leclerc, Hamilton
    Mercedes: Russell, Antonelli
    Red Bull: Verstappen, Hadjar
    Williams: Sainz, Albon
    Aston Martin: Alonso, Stroll
    Alpine: Gasly (Colapinto is player — DO NOT include as rival)
    Haas: Ocon, Bearman
    Audi: Bortoleto, Hülkenberg
    Racing Bulls: Lawson, Lindblad
    Cadillac: Bottas, Pérez

    Skill tiers per D-04:
    Top tier (0.92–0.96): Verstappen=0.96, Norris=0.93, Hamilton=0.94, Leclerc=0.92, Piastri=0.92
    Mid tier (0.86–0.91): Russell=0.90, Antonelli=0.87, Sainz=0.88, Alonso=0.91, Gasly=0.87, Lawson=0.86
    Lower tier (0.79–0.85): Albon=0.83, Stroll=0.81, Ocon=0.84, Bearman=0.79, Bortoleto=0.80, Hülkenberg=0.84, Hadjar=0.82, Bottas=0.82, Pérez=0.83, Lindblad=0.80

    Array field structure (unchanged from existing):
    { name: string, team: string, num: string, body: string, accent: string, helmet: string, skill: number }

    Ordering: sort easiest → hardest by skill tier (matches existing pattern with section comments MEDIO / DURO / EXPERTO / ÉLITE).

    After replacing the array, add a code comment above it:
    // 2026 F1 grid — 21 rivals (Colapinto is the player, not listed here)
    // Colors verified at formula1.com on [date]. localStorage keys cr_rival_<0-20>.
    // Note: index positions changed from v1 — cr_rival_* keys from old grid are orphaned (D-05, accepted).

    Do NOT change buildRivalGrid() — it iterates RIVALS by index and works with any length.
  </action>
  <verify>
    Open index.html in browser. Click "VS CPU": rival grid should show 21 cards (not 20). Verify:
    - Colapinto does NOT appear as a rival card
    - Cadillac team appears (Bottas and Pérez cards visible)
    - "Audi" appears as team name (not "Kick Sauber") on Bortoleto and Hülkenberg
    - No rival card shows an empty name, team, or number
    In console: no errors.
  </verify>
  <acceptance_criteria>
    - game.js RIVALS array contains exactly 21 object entries (count opening braces in the array)
    - The string "Franco Colapinto" does NOT appear in the RIVALS array
    - The string "Yuki Tsunoda" does NOT appear in the RIVALS array (unless formula1.com confirms 2026 seat)
    - The string "Cadillac" appears in the RIVALS array (two entries: Bottas and Pérez)
    - The string "Kick Sauber" does NOT appear in the RIVALS array (replaced by "Audi")
    - RIVALS array has the comment "2026 F1 grid" above it
    - Browsing to VS CPU shows 21 rival cards in the grid
  </acceptance_criteria>
  <done>RIVALS array has 21 entries with verified 2026 team names, livery colors, car numbers, and skill tiers. No Colapinto or Tsunoda entries. Cadillac team present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → onMsg() | PeerJS messages from remote peer arrive as untrusted JSON |
| localStorage → RIVALS index | cr_rival_<idx> keys are read from storage; index shift after grid update can cause display mismatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | onMsg finish handler (game.js ~992) | mitigate | BUG-02 guard: `if (remote.lap < TOTAL_LAPS - 1) return;` prevents premature win via spoofed finish message |
| T-01-02 | Spoofing | localStorage cr_rival_* keys | accept | Index shift from grid reorder causes display mismatch but no security impact; per D-05 orphaned keys are accepted |
| T-01-03 | Denial of Service | alert() in onDisconnect | accept in this plan | alert() blocks JS thread; replacement is in plan 04 (UI-06). No sensitive data exposed |
| T-01-SC | Tampering | npm/pip installs | accept | No package manager installs in this plan — pure game.js edits only |
</threat_model>

<verification>
1. Load index.html in browser — no console errors.
2. Click VS CPU — 21 rival cards appear, Colapinto absent, Cadillac present.
3. Select any rival, complete race, click REVANCHA — race restarts once cleanly.
4. During race: press spacebar — car brakes. Release — car accelerates. Page does not scroll.
5. Arrow keys and WASD continue to work (regression check for CTRL-01/02).
</verification>

<success_criteria>
- Zero SyntaxErrors or ReferenceErrors in browser console on page load
- RIVALS array has exactly 21 entries with 2026 grid (formula1.com verified)
- Spacebar triggers brake (keys.down = true) with page-scroll prevention
- Solo REVANCHA triggers exactly one beginCountdown() call (no double reset)
- cpScore lambda defined exactly once in updateHUD()
- lapStartTime initialized before first CP0 crossing (lap 1 timed correctly)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done.
</output>
