---
phase: 02-monaco-4-cars
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified: [game.js]
autonomous: false
requirements: [CARS-02, CARS-03, CARS-04]

must_haves:
  truths:
    - "Los 3 autos IA tienen personalidades distintas (agresivo, defensivo, consistente) que afectan su velocidad y línea"
    - "Cuando dos autos cualesquiera de los 4 colisionan, ambos son desviados físicamente (6 pares cubiertos)"
    - "El HUD muestra la clasificación P1/P2/P3/P4 en tiempo real y se actualiza cuando los autos se adelantan"
  artifacts:
    - path: "game.js"
      provides: "PERSONALITIES, asignación de personalidad por auto, loop de colisión de 6 pares, HUD P1-P4"
      contains: "PERSONALITIES"
  key_links:
    - from: "PAIRS"
      to: "resolveCarCollision(cars[i], cars[j])"
      via: "loop sobre los 6 pares en la fase racing"
      pattern: "PAIRS"
    - from: "updateAI(car)"
      to: "car.personality"
      via: "los multiplicadores de personalidad modulan velocidad/línea/freno"
      pattern: "personality"
    - from: "updateHUD()"
      to: "hudPos"
      via: "cpScore rankea 4 autos -> P1-P4 del jugador"
      pattern: "hudPos"
---

<objective>
Dar a los 3 autos IA personalidades de carrera distintas (agresivo, defensivo, consistente) que modulen su comportamiento, extender el sistema de colisiones a los 6 pares posibles entre los 4 autos, y mostrar la clasificación P1/P2/P3/P4 en vivo en el HUD.

Purpose: Cierra los requisitos restantes de la Fase 2. Las personalidades dan carácter a los rivales (core value del proyecto), las colisiones de 6 pares completan la física multi-auto, y el HUD P1-P4 comunica la posición del jugador entre 4 autos.
Output: `game.js` con PERSONALITIES, asignación por auto, colisión de 6 pares con daño al jugador, y HUD P1-P4 con detección de adelantamiento.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-monaco-4-cars/02-RESEARCH.md
@CLAUDE.md
@game.js
@.planning/phases/02-monaco-4-cars/02-03-SUMMARY.md

<interfaces>
<!-- Funciones que este plan extiende. Tras 02-01 ya operan sobre cars[]. -->
updateAI(car, dt)        : navega waypoints; usa car.rivalData.skill. Agregar car.personality.
resolveCarCollision(a,b) (game.js:552) : ya genérico; reusar para los 6 pares.
updateHUD()              : tras 02-01 rankea con cpScore; aquí completar P1-P4.
hudPos (DOM)             : <div id="hud-pos"> ya muestra "P1" en index.html:72.
cpScore                  : lambda en updateHUD: c.finished?Infinity : c.lap*CPS.length + (c.nextCP===0?CPS.length:c.nextCP)
resetGame()              : tras 02-01 construye cars[] y asigna car.rivalData; aquí agregar car.personality.
addFloatingText(text,color,x,y,size) (game.js:791) : para celebración de adelantamiento.
</interfaces>

<personalities_reference>
PERSONALITIES y estrategia de asignación: 02-RESEARCH.md líneas 288-324.
3 tipos: aggressive (speedMult 1.05, lineMult 0.7, brakeMult 0.8, damageMult 1.5),
defensive (speedMult 0.92, lineMult 1.3, brakeMult 1.2, damageMult 0.8),
consistent (speedMult 1.0, lineMult 1.0, brakeMult 1.0, damageMult 1.0).
Asignar una personalidad a cada uno de los 3 autos IA (una de cada tipo por carrera).
Valores [ASSUMED] — requieren playtesting (Assumption A4).
</personalities_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Personalidades de IA (CARS-02) y colisión de 6 pares (CARS-03)</name>
  <files>game.js</files>
  <read_first>
    - game.js (updateAI tras refactor, resolveCarCollision 552-569, resetGame tras 02-01, loop fase racing colisión)
    - .planning/phases/02-monaco-4-cars/02-RESEARCH.md ("AI Personalities" 283-324, "Collision System Extension" 395-439)
    - .planning/phases/02-monaco-4-cars/02-01-SUMMARY.md (cómo quedó la asignación de rivalData por auto en resetGame)
  </read_first>
  <action>
    Definir un objeto constante `PERSONALITIES` cerca de las constantes de juego, con las 3 personalidades (aggressive, defensive, consistent) y sus multiplicadores según 02-RESEARCH.md (líneas 288-313): `speedMult`, `lineMult`, `noiseAmp`, `brakeMult`, `damageMult`, y `style` (string del nombre).

    En `resetGame()` (ya construye cars[] en 02-01), asignar una personalidad a cada uno de los 3 autos IA: una de cada tipo por carrera (`['aggressive','defensive','consistent']` mapeado a cars[1], cars[2], cars[3]). Guardar el objeto en `car.personality` y conservar `car.rivalData.skill` para la velocidad base. Guardar también `car.personality.style` accesible para HUD/resultados.

    Modificar `updateAI(car, dt)` para aplicar los multiplicadores de `car.personality` (con fallback a consistent si falta):
    - Velocidad máxima: multiplicar la `aiMaxSpd` existente por `car.personality.speedMult`.
    - Freno: multiplicar el `BRAKE_FORCE * 0.35` existente por `car.personality.brakeMult`.
    - Ruido/variación lateral: usar `car.personality.noiseAmp` en lugar del cálculo fijo basado solo en skill.
    - Línea (lineMult): aplicar un offset lateral al waypoint objetivo proporcional a `lineMult` (agresivo=línea más interior/ajustada, defensivo=más amplia). Mantener el efecto sutil para no sacar a la IA de pista.
    NOTA: el frenado real más fuerte (AI-01) y la variación por vuelta avanzada (AI-02, AI-03) son de la Fase 3 — aquí solo aplicar los multiplicadores de personalidad. No cambiar el `0.35` base a `0.70` (eso es AI-01, Fase 3).

    Definir `const PAIRS = [[0,1],[0,2],[0,3],[1,2],[1,3],[2,3]];` y reemplazar la colisión jugador-vs-cars[1] (la que 02-01 dejó como única) por un loop sobre los 6 pares en la fase racing del loop (solo modo solo; en multi sigue siendo 2 autos = par [0,1]). Para cada par `[i,j]`: saltar si ambos `finished`; calcular la velocidad de aproximación antes de resolver; llamar `resolveCarCollision(cars[i], cars[j])`; si hubo colisión y el par involucra a `cars[0]` (jugador), acumular daño en `cars[0].damage` usando `car.personality.damageMult` del auto IA involucrado para escalar el daño (agresivo pega más fuerte), y reproducir `playCollisionSound()`. Las colisiones IA-vs-IA solo desvían (sin daño al HUD; sonido opcional si cerca del jugador). Conservar el modelo de daño base (`baseDmg = Math.min(6, 1 + relV*0.02)`, factor 0.15 si el jugador no es agresor) de 02-RESEARCH.md líneas 412-436.

    Para multi mode (2 autos): mantener únicamente el par [0,1] — no iterar PAIRS completo cuando `cars.length === 2`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('game.js','utf8'); if(!/PERSONALITIES/.test(s)){console.error('FAIL: falta PERSONALITIES');process.exit(1)} if(!/\['?aggressive'?|aggressive:/.test(s)){console.error('FAIL: falta personalidad aggressive');process.exit(1)} const pairs=s.match(/PAIRS\s*=\s*\[([\s\S]*?)\]\s*;/); if(!pairs){console.error('FAIL: falta PAIRS');process.exit(1)} const n=(pairs[1].match(/\[\s*\d\s*,\s*\d\s*\]/g)||[]).length; if(n!==6){console.error('FAIL: PAIRS no tiene 6 pares, tiene',n);process.exit(1)} console.log('OK PERSONALITIES y 6 pares')"</automated>
    <human-check>En VS CPU, observar varias vueltas: los 3 autos IA tienen ritmos/líneas notoriamente distintos (uno más rápido y agresivo, uno más conservador). Provocar colisiones entre el jugador y cada IA, y entre dos IAs: en todos los casos ambos autos se desvían físicamente. La barra de daño del jugador sube al chocar.</human-check>
  </verify>
  <acceptance_criteria>
    - Existe `PERSONALITIES` con las claves `aggressive`, `defensive` y `consistent`, cada una con `speedMult`, `lineMult`, `noiseAmp`, `brakeMult`, `damageMult`.
    - `resetGame()` asigna `car.personality` a cars[1], cars[2] y cars[3] (una de cada tipo).
    - `updateAI` usa `car.personality.speedMult` y `car.personality.brakeMult` (sin cambiar el factor base 0.35 de freno).
    - `const PAIRS` contiene exactamente los 6 pares `[0,1],[0,2],[0,3],[1,2],[1,3],[2,3]`.
    - La fase racing en solo itera los 6 pares llamando `resolveCarCollision`; en multi solo el par [0,1].
    - El daño al jugador escala con el `damageMult` del IA agresor.
    - Verificación manual: 3 IAs con comportamiento distinto; colisiones desvían ambos autos en cualquier par.
  </acceptance_criteria>
  <done>3 personalidades de IA aplicadas; colisión de 6 pares operativa con daño al jugador modulado por personalidad.</done>
</task>

<task type="auto">
  <name>Task 2: HUD de clasificación P1/P2/P3/P4 en vivo (CARS-04)</name>
  <files>game.js</files>
  <read_first>
    - game.js (updateHUD tras 02-01, cpScore, hudPos ref línea 141, prevIsFirst/prevPlayerRank, addFloatingText 791)
    - .planning/phases/02-monaco-4-cars/02-RESEARCH.md ("P4 HUD Classification" 443-479, Pitfall 6 desempate 678-683)
    - index.html (hud-pos línea 72 — ya muestra "P1")
  </read_first>
  <action>
    Completar `updateHUD()` para clasificar los 4 autos en tiempo real:
    - Rankear todos los autos de `cars[]` usando la lambda `cpScore` existente, ordenando de mayor a menor score (mayor score = más adelante).
    - Desempate (Pitfall 6): si dos autos tienen el mismo `cpScore`, el que esté físicamente más cerca de su próximo checkpoint rankea más alto. Implementar la distancia euclidiana al `CPS[car.nextCP]` como criterio secundario para estabilizar el ranking (evitar parpadeo P1/P2 cuando van lado a lado).
    - La posición del jugador es el índice (1-based) de `cars[0]` en el array rankeado. Mostrar `hudPos.textContent = 'P' + playerRank` (P1, P2, P3 o P4).
    - Detección de adelantamiento: reemplazar la variable `prevIsFirst` por `prevPlayerRank`. Si `playerRank < prevPlayerRank` (el jugador subió de posición), llamar `addFloatingText('¡LO PASÉ! ⚡', '#10b981', 240, 220, 20)`. Actualizar `prevPlayerRank = playerRank` al final. Inicializar `prevPlayerRank` en `resetGame()` (por ejemplo a 4, la última posición, o al rank inicial del jugador).
    - Gap display (`hudRole`) en solo: conservar un texto de gap útil — comparar `cpScore(cars[0])` contra el auto inmediatamente delante o detrás en el ranking, mostrando la diferencia aproximada en segundos como hoy. No debe crashear con 4 autos.
    - En multi (2 autos) el ranking sigue funcionando: P1/P2.

    Asegurar que la inicialización de `prevPlayerRank` quede en `resetGame()` y que no queden referencias a `prevIsFirst`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('game.js','utf8'); const code=s.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n'); if(/\bprevIsFirst\b/.test(code)){console.error('FAIL: queda prevIsFirst');process.exit(1)} if(!/prevPlayerRank/.test(s)){console.error('FAIL: falta prevPlayerRank');process.exit(1)} if(!/hudPos\.textContent\s*=\s*[`'\"]?P/.test(s) && !/'P'\s*\+\s*playerRank/.test(s) && !/`P\$\{/.test(s)){console.error('FAIL: hudPos no muestra P+rank');process.exit(1)} console.log('OK HUD P1-P4')"</automated>
    <human-check>En VS CPU, observar el HUD durante la carrera: muestra P1/P2/P3/P4 y cambia en tiempo real cuando el jugador adelanta o es adelantado por una IA. Al adelantar aparece el floating text "¡LO PASÉ!". El indicador no parpadea cuando dos autos van lado a lado.</human-check>
  </verify>
  <acceptance_criteria>
    - `updateHUD()` rankea los 4 autos por `cpScore` con desempate por distancia al próximo checkpoint.
    - `hudPos.textContent` se asigna como `P` seguido del rank del jugador (P1..P4).
    - La detección de adelantamiento usa `prevPlayerRank` (no `prevIsFirst`); `prevIsFirst` no aparece en el código.
    - `prevPlayerRank` se inicializa en `resetGame()`.
    - Verificación manual: HUD muestra P1-P4 actualizándose en vivo; floating text al adelantar; sin parpadeo lado a lado.
  </acceptance_criteria>
  <done>HUD muestra clasificación P1-P4 en tiempo real con desempate estable y celebración de adelantamiento.</done>
</task>

</tasks>

<verification>
- PERSONALITIES con 3 tipos; cada IA tiene una personalidad; comportamientos distintos observables.
- PAIRS con 6 pares; colisiones desvían ambos autos en cualquier combinación; daño al jugador modulado por personalidad.
- HUD muestra P1-P4 en vivo con desempate estable y floating text al adelantar.
- Multi 1v1 sigue funcionando (P1/P2, par [0,1]).
</verification>

<success_criteria>
Los 3 autos IA exhiben personalidades distintas. El sistema de colisiones cubre los 6 pares entre los 4 autos. El HUD muestra la clasificación P1/P2/P3/P4 en tiempo real. La Fase 2 queda completa.
</success_criteria>

<output>
Crear `.planning/phases/02-monaco-4-cars/02-04-SUMMARY.md` al terminar.
</output>
