---
phase: 02-monaco-4-cars
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [game.js]
autonomous: true
requirements: [CARS-01]

must_haves:
  truths:
    - "El juego corre en modo solo con 4 autos en pista (jugador + 3 IAs) sin errores en consola"
    - "El juego corre en modo multijugador 1v1 (2 autos) exactamente como antes del refactor"
    - "Cada auto IA navega de forma independiente (no se mueven en bloque sincronizado)"
    - "El jugador puede completar una carrera de 3 vueltas en modo solo y llegar a la pantalla de resultados"
  artifacts:
    - path: "game.js"
      provides: "Array cars[] reemplaza local/remote; estado de daño y waypoint por auto"
      contains: "let cars"
  key_links:
    - from: "resetGame()"
      to: "cars[]"
      via: "inicializacion del array segun gameMode (4 en solo, 2 en multi)"
      pattern: "cars\\s*="
    - from: "loop() racing phase"
      to: "cars[]"
      via: "forEach actualiza cada auto (updateCar player / updateAI IA)"
      pattern: "cars\\.forEach|cars\\[0\\]"
    - from: "onMsg('pos')"
      to: "cars[1]"
      via: "auto remoto en multijugador escribe en cars[1]"
      pattern: "cars\\[1\\]"
---

<objective>
Refactorizar el modelo de estado de autos de las dos variables fijas `local`/`remote` a un array `cars[]`, soportando 4 autos en modo solo (jugador + 3 IAs) y 2 autos en modo multijugador (jugador + rival remoto). Este es el refactor load-bearing del que depende todo el resto de la Fase 2.

Purpose: Habilitar carreras de 4 autos sin romper el multijugador 1v1 existente. Cada otro requisito de la Fase 2 (Monaco, colisiones, personalidades, HUD P1-P4) lee este modelo de datos.
Output: `game.js` con `cars[]` reemplazando `local`/`remote` en los 30+ puntos de uso, estado de daño y waypoint movidos a cada objeto auto, y modo multijugador funcionando idéntico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-monaco-4-cars/02-RESEARCH.md
@CLAUDE.md
@game.js

<interfaces>
<!-- Contratos actuales en game.js que este refactor toca. El executor debe respetarlos. -->

Estado actual (game.js:156-175):
- makeCar(idx) -> { x, y, angle, speed, lap, nextCP, finished }   (idx indexa START[])
- let local  = makeCar(0)
- let remote = makeCar(1) + { prevX, prevY, prevAngle, lastUpdate }
- let winner = null         // actualmente 'local' | 'remote'
- let localDamage = 0       // daño del jugador
- let aiWpIdx = 0           // indice de waypoint AI compartido (global)

Funciones que reciben/usan un auto:
- checkCheckpoints(car)     // game.js:572 — compara con `car === local` para timing/flash
- updateCar(car, dt, damage)// game.js:611 — usa keys.* (input del jugador)
- updateAI(car, dt)         // game.js:639 — usa aiWpIdx global y selectedRival.skill
- updateHUD()               // game.js:679 — usa local y remote, cpScore
- drawCar(car, styleIdx)    // game.js:442 — styleIdx 0=jugador, 1=rival
- carStyle(styleIdx)        // game.js:436 — 0=CAR_STYLE_HOST, else selectedRival
- remoteRenderPos()         // game.js:524 — interpola remote para multijugador
- resolveCarCollision(a, b) // game.js:552 — par de autos, ya generico

Red multijugador (game.js:982-1011): onMsg('pos') escribe remote.x/y/angle/speed/lap/nextCP.
Resultado (game.js:1346-1348, pollResults): `won = winner === 'local'`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introducir array cars[] y migrar estado, inicialización y red</name>
  <files>game.js</files>
  <read_first>
    - game.js (archivo completo — el refactor toca 30+ puntos; el executor debe ver el estado real, no asumirlo)
    - .planning/phases/02-monaco-4-cars/02-RESEARCH.md (seccion "CARS-01 Refactor Blueprint" — inventario completo de touch-points, líneas 199-281)
  </read_first>
  <action>
    Reemplazar las variables globales `local` y `remote` (game.js:161-165) por un array `let cars = [];`.

    Mover el estado por-auto adentro de cada objeto en `makeCar(idx)`: agregar `isPlayer: false`, `damage: 0`, `wpIdx: 0` al objeto que retorna `makeCar`. Eliminar las globales `let localDamage = 0;` (game.js:174) y `let aiWpIdx = 0;` (game.js:175). Toda referencia a `localDamage` pasa a `cars[0].damage`; toda referencia a `aiWpIdx` pasa a `car.wpIdx`.

    Cambiar `let winner = null;` para que almacene un índice numérico (0-3) o `null` en vez de los strings `'local'`/`'remote'`. Donde antes `winner = 'local'` ahora es `winner = 0`; donde `winner = 'remote'` ahora es el índice del auto que ganó (en multijugador `cars[1]`, índice 1).

    Reescribir `resetGame()` (game.js:1036-1065) para construir `cars[]` según `gameMode`:
    - Modo 'solo': array de longitud 4. `cars[0]` = jugador con `isPlayer: true`, `damage: 0`, `wpIdx: 0`. `cars[1]`, `cars[2]`, `cars[3]` = IAs con `isPlayer: false`, `damage: 0`, y `wpIdx` escalonado (cars[1].wpIdx=0, cars[2].wpIdx=2, cars[3].wpIdx=4) para evitar que arranquen en bloque. Los 3 autos IA deben tener posiciones de salida distintas — usar START[1] como base y desplazar; como START solo tiene 2 entradas actualmente, agregar entradas START[2] y START[3] (desplazadas ~28px lateral / ~6px longitudinal respecto a START[0]/START[1], pointing east angle 0). Para la selección de los 3 rivales IA en solo usar la regla del research: pivote en `selectedRivalIdx`, tomar índices vecinos en RIVALS (pivotIdx-1, pivotIdx+1, pivotIdx+2) deduplicados y clampeados a [0, RIVALS.length-1]; guardar el rival elegido por el jugador como uno de los tres. Guardar el objeto rival usado por cada IA en `car.rivalData` para que `carStyle` lo lea.
    - Modo 'multi': array de longitud 2. `cars[0]` = jugador local (con `isPlayer: true`). `cars[1]` = rival remoto con los campos de interpolación de red `prevX, prevY, prevAngle, lastUpdate`. Respetar el mapeo host/guest existente: el jugador local usa START[isHost ? 0 : 1] y el remoto START[isHost ? 1 : 0].

    Migrar `onMsg('pos')` (game.js:992-1001): `remote.prevX` -> `cars[1].prevX`, etc. Toda escritura del auto remoto va a `cars[1]`. Migrar `onMsg('finish')` (game.js:1004-1011): `remote.lap` -> `cars[1].lap`, `winner = 'remote'` -> `winner = 1`.

    Migrar `remoteRenderPos()` (game.js:524-532) para que lea `cars[1]` en vez de `remote`.

    NO incluir bloques de código cerrados (```), trabajar directamente sobre game.js con Edit.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('game.js','utf8'); const bad=s.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n'); if(/\blet\s+local\b|\blet\s+remote\b|\blocalDamage\b|\baiWpIdx\b/.test(bad)){console.error('FAIL: quedan referencias a estado viejo');process.exit(1)} if(!/let\s+cars\s*=/.test(s)){console.error('FAIL: no se declara cars[]');process.exit(1)} console.log('OK estado migrado')"</automated>
  </verify>
  <acceptance_criteria>
    - `game.js` contiene la declaración `let cars =` y NO contiene `let local`, `let remote`, `localDamage` ni `aiWpIdx` (fuera de comentarios).
    - `makeCar()` retorna un objeto con las propiedades `isPlayer`, `damage` y `wpIdx` además de las originales.
    - `winner` se asigna con valores numéricos (0,1,2,3) o `null`, nunca con los strings `'local'`/`'remote'`.
    - `resetGame()` construye `cars` con longitud 4 cuando `gameMode === 'solo'` y longitud 2 cuando `gameMode === 'multi'`.
    - `cars[2].wpIdx` y `cars[3].wpIdx` tienen valores iniciales distintos de `cars[1].wpIdx` (escalonados).
    - `onMsg('pos')` escribe en `cars[1]` y nunca referencia `remote`.
    - El array `START` tiene al menos 4 entradas con coordenadas distintas.
  </acceptance_criteria>
  <done>El estado de autos vive en `cars[]`; inicialización por modo correcta; red multijugador apunta a `cars[1]`.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar loop, físicas, IA, checkpoints, HUD, carStyle y polling de resultados a cars[]</name>
  <files>game.js</files>
  <read_first>
    - game.js (archivo completo, ya parcialmente modificado por Task 1)
    - .planning/phases/02-monaco-4-cars/02-RESEARCH.md (seccion "Extended loop() racing phase structure", líneas 736-779)
  </read_first>
  <action>
    Migrar `updateAI(car, dt)` (game.js:639-676) para que use `car.wpIdx` en lugar de la global `aiWpIdx`: leer `AI_WAYPOINTS[car.wpIdx]` y avanzar con `car.wpIdx = (car.wpIdx + 1) % AI_WAYPOINTS.length`. La fuente de skill pasa de `selectedRival.skill` a `car.rivalData ? car.rivalData.skill : 0.88` (la personalidad completa se agrega en el plan 02-04; por ahora usar el skill del rivalData del auto). NO usar `selectedRival` global dentro de updateAI.

    Migrar `updateCar(car, dt, damage)` (game.js:611-634): el chequeo de freno `if (keys.down)` debe gatear sobre el jugador — usar `if (car.isPlayer && keys.down)`. Igual para steering (`keys.left`/`keys.right` solo aplican si `car.isPlayer`).

    Migrar `checkCheckpoints(car)` (game.js:572-608): cambiar todas las comparaciones `car === local` por `car.isPlayer`. El timing de vuelta, flash de checkpoint y floating texts solo ocurren cuando `car.isPlayer` es true (solo el jugador). Los autos IA actualizan lap/nextCP en silencio.

    Reescribir la fase `racing` del `loop()` (game.js:836-943):
    - Modo solo: `cars.forEach(car => { car.isPlayer ? updateCar(car, dt, car.damage) : updateAI(car, dt); checkCheckpoints(car); });`
    - Modo multi: actualizar solo `cars[0]` con updateCar + checkCheckpoints; el broadcast de red de posición usa `cars[0]` (`x: cars[0].x`, etc.); `cars[1]` se actualiza vía onMsg.
    - Colisión: en este plan, conservar SOLO la colisión jugador-vs-cars[1] en modo solo (migrada de la lógica actual local/remote a `cars[0]`/`cars[1]`), usando `cars[0].damage` para el daño. El sistema completo de 6 pares se agrega en el plan 02-04 — dejar un comentario `// CARS-03: extender a 6 pares en 02-04`.
    - Audio del motor y freno: gatear sobre `cars[0]` (`updateEnginePitch(cars[0].speed)`, freno con `cars[0].speed`).
    - Render: dibujar todos los autos. En solo, dibujar cars[3], cars[2], cars[1] y por último cars[0] (jugador arriba). En multi, dibujar cars[1] (interpolado vía remoteRenderPos) y luego cars[0]. Mantener el mapeo de estilo correcto.
    - Off-track vignette, acumulación de daño y shake: gatear sobre `cars[0]` (solo el jugador), reemplazando `local` por `cars[0]` y `localDamage` por `cars[0].damage`.
    - Detección de ganador: `if (cars[0].damage >= 100) winner = ...` (auto destruido — usar índice de un IA o un valor que pollResults interprete como derrota); recorrer `cars` y asignar `winner = i` del primer auto con `car.finished`. Si `cars[0].finished` primero -> winner=0. En multi, enviar `Net.send({ type:'finish' })` cuando `cars[0].finished`.

    Reescribir `updateHUD()` (game.js:679-716): rankear TODOS los autos por la lambda `cpScore` existente (ya correcta), ordenar descendente. La posición del jugador es el rank de `cars[0]`. Por ahora mostrar `hudPos.textContent = isFirst ? '1°' : '2°'` adaptado a N autos NO es el objetivo de este plan — el HUD P1-P4 completo es 02-04. Aquí solo asegurar que updateHUD no rompa con cars[] (no referenciar local/remote). El gap display `hudRole` en solo: comparar `cars[0]` contra el auto líder rival; conservar comportamiento equivalente sin crashear. Dejar comentario `// CARS-04: clasificacion P1-P4 completa en 02-04`.

    Migrar `carStyle(styleIdx)` y `drawCar` (game.js:436-521): `carStyle` debe aceptar el objeto auto o su índice. Para `cars[0]` retornar `CAR_STYLE_HOST`. Para autos IA, derivar el estilo de `car.rivalData` (body/accent/helmet/num). Ajustar las llamadas a `drawCar` para pasar la info de estilo correcta por auto.

    Migrar `pollResults()` (game.js:1346-1348): `won = winner === 'local'` -> `won = winner === 0`. El resto del texto sigue refiriendo al jugador (Colapinto). El guardado de resultado por rival usa `selectedRivalIdx` (sin cambios).

    Migrar las fases `countdown` (game.js:819-824) y `done` (game.js:944-949) para dibujar todos los autos de `cars[]` en vez de local/remote.

    Tras todos los cambios, hacer un barrido de texto buscando `\blocal\b` y `\bremote\b` y `selectedRival\b` dentro de funciones de juego; el único superviviente válido de `selectedRival` es la selección de rivales en buildRivalGrid y resetGame.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('game.js','utf8'); const code=s.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n'); const refs=(code.match(/\b(local|remote)\b/g)||[]); if(refs.length){console.error('FAIL: quedan',refs.length,'referencias local/remote en codigo');process.exit(1)} if(/winner\s*===?\s*'(local|remote)'/.test(code)){console.error('FAIL: comparacion winner con string');process.exit(1)} console.log('OK loop migrado, 0 refs local/remote')"</automated>
    <human-check>Abrir index.html, jugar una carrera VS CPU completa (3 vueltas): los 4 autos aparecen y se mueven de forma independiente, el jugador completa la carrera y aparece la pantalla de resultados sin errores en la consola. Luego probar multijugador 1v1 (2 pestañas): ambos autos se ven y la carrera termina correctamente.</human-check>
  </verify>
  <acceptance_criteria>
    - Fuera de comentarios, `game.js` NO contiene las palabras `local` ni `remote` como identificadores (0 coincidencias del regex `\b(local|remote)\b` en código).
    - `winner` nunca se compara contra `'local'` ni `'remote'`; `pollResults` usa `winner === 0`.
    - `updateAI` lee `AI_WAYPOINTS[car.wpIdx]` y avanza `car.wpIdx`, sin referenciar `aiWpIdx`.
    - `updateCar` gatea freno/steering con `car.isPlayer`.
    - `checkCheckpoints` usa `car.isPlayer` (no `car === local`) para el timing y los efectos del jugador.
    - El loop fase racing en solo actualiza los 4 autos vía `cars.forEach`.
    - `carStyle` deriva estilo de IA desde `car.rivalData`.
    - Verificación manual: 4 autos visibles y móviles en VS CPU; multijugador 1v1 sigue funcionando; consola sin errores.
  </acceptance_criteria>
  <done>Todo el game loop, físicas, IA, HUD, render y resultados operan sobre `cars[]`; cero referencias residuales a local/remote; solo y multi funcionales.</done>
</task>

</tasks>

<verification>
- Búsqueda de texto: 0 coincidencias de `\blocal\b` y `\bremote\b` como identificadores en game.js (fuera de comentarios).
- Modo solo: 4 autos aparecen en la grilla, se mueven independientes, el jugador puede terminar 3 vueltas.
- Modo multi: 2 autos, carrera 1v1 termina igual que antes del refactor.
- Consola del navegador sin TypeError durante el primer frame de racing.
</verification>

<success_criteria>
El modelo `local`/`remote` está completamente reemplazado por `cars[]`. El modo solo soporta 4 autos (jugador + 3 IAs independientes). El modo multijugador 1v1 funciona idéntico. No quedan referencias residuales al estado viejo.
</success_criteria>

<output>
Crear `.planning/phases/02-monaco-4-cars/02-01-SUMMARY.md` al terminar.
</output>
